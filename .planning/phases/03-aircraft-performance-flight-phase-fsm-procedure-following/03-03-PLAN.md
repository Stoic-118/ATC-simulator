---
phase: 03-aircraft-performance-flight-phase-fsm-procedure-following
plan: 03
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - src/atc_sim/sim/phase.py
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "Phase enum has exactly 8 members: TAXI_OUT, DEPARTURE_ROLL, CLIMB, ENROUTE, DESCENT, APPROACH, LANDED, TAXI_IN"
    - "LEGAL_TRANSITIONS has a key for every Phase member"
    - "transition_to raises ValueError on any transition not in the table; the full linear walk succeeds"
  artifacts:
    - src/atc_sim/sim/phase.py
  key_links:
    - "phase.py imports nothing from atc_sim.sim.* — it is a pure leaf, independently unit-testable with zero Aircraft construction"
    - "No LOC_CAPTURED / GS_CAPTURED / REMOVED members (ILS capture is Phase 4; removal is list membership, not a phase)"
---

## Phase Goal

**As a** controller observing the radar, **I want to** watch aircraft transition through legal, never-skipped flight phases, **so that** the simulated lifecycle is trustworthy before Phase 4 instructions exist.

<objective>
Deliver the explicit flight-phase state machine (PERF-02): an 8-member `Phase` enum matching the roadmap's success-criterion #4 wording exactly, a `LEGAL_TRANSITIONS` table, and a `transition_to` guard that rejects illegal transitions. This makes `tests/test_phase_fsm.py` activate and pass.

Purpose: Guarantee aircraft phase transitions are always legal and observable (success criterion #4) via a single authoritative enum field rather than scattered booleans (ARCHITECTURE Pattern 2 / Anti-Pattern 2).
Output: `src/atc_sim/sim/phase.py`.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md
@src/atc_sim/sim/clock.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase enum, LEGAL_TRANSITIONS table, transition_to guard</name>
  <files>src/atc_sim/sim/phase.py</files>
  <read_first>
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md (Pattern B lines 262-306 — the exact 8-state enum, the transitions table, transition_to, and the "spawn phase depends on flight kind" and "Removal is not a phase" notes)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("phase.py" section — no in-repo FSM analog exists; use RESEARCH's example directly; LEGAL_TRANSITIONS placed module-level directly below the enum like navdata/models.py's RestrictionKind)
    - src/atc_sim/sim/clock.py (module docstring style + "small pure function + module-level constant table" structure to mirror)
  </read_first>
  <behavior>
    - Phase has exactly 8 members with the specified names
    - LEGAL_TRANSITIONS keys cover all 8 members; TAXI_IN maps to the empty set (terminal)
    - transition_to(TAXI_OUT, DEPARTURE_ROLL) succeeds; transition_to(TAXI_OUT, CLIMB) raises ValueError
    - Walking the full chain TAXI_OUT->...->TAXI_IN via transition_to raises nothing
  </behavior>
  <action>
    Create `src/atc_sim/sim/phase.py`. Begin with the mandatory sim-core docstring ending in the exact sentence "This module MUST NOT import pygame — it stays headlessly testable via pytest with no display, enforced by tests/test_boundary.py." (verbatim wording from src/atc_sim/sim/aircraft.py).

    Define `class Phase(Enum)` using `from enum import Enum, auto` with exactly these 8 members in this order: TAXI_OUT, DEPARTURE_ROLL, CLIMB, ENROUTE, DESCENT, APPROACH, LANDED, TAXI_IN. Do NOT add LOC_CAPTURED, GS_CAPTURED, or a REMOVED/GONE member — ILS capture is Phase 4 (PROC-03) and removal-from-traffic is a list-membership concern handled in plan 03-05, not a phase (RESEARCH "Removal-from-active-traffic treated as a phase-FSM transition" pitfall).

    Define `LEGAL_TRANSITIONS: dict[Phase, set[Phase]]` directly below the enum, mapping each phase to the single next phase in the linear chain, with TAXI_IN mapping to an empty set. Add the comment block from RESEARCH lines 283-287 explaining that ENROUTE->DESCENT and DESCENT->APPROACH exist for forward-compatibility even though this phase's departure demo is removed from ENROUTE and its arrival demo spawns directly into DESCENT.

    Define `transition_to(current: Phase, new: Phase) -> Phase` that raises `ValueError(f"Illegal transition {current} -> {new}")` when `new not in LEGAL_TRANSITIONS[current]`, else returns `new`.

    IMPORTANT: Keep this module a pure leaf — do NOT import `Aircraft` or any `atc_sim.sim.*` module. Completion-condition logic that reads aircraft state (is-this-phase-done) belongs in aircraft.py's dispatch in plan 03-04, so phase.py stays independently testable with zero Aircraft construction (RESEARCH structure rationale). This also avoids a circular import between phase.py and aircraft.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase_fsm.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/atc_sim/sim/phase.py` exists with the "MUST NOT import pygame" docstring sentence and no import of `atc_sim.sim.*`
    - `Phase` has exactly 8 members with the exact names/order specified; no LOC_CAPTURED/GS_CAPTURED/REMOVED member
    - `LEGAL_TRANSITIONS` has a key for all 8 members; `LEGAL_TRANSITIONS[Phase.TAXI_IN] == set()`
    - `transition_to(Phase.TAXI_OUT, Phase.CLIMB)` raises `ValueError`; the full linear walk raises nothing
    - `python -m pytest tests/test_phase_fsm.py -x -q` passes (file activates from skipped)
    - `python -m pytest tests/test_boundary.py -x -q` passes (phase.py stays headless)
  </acceptance_criteria>
  <done>An 8-state FSM with a guarded transition function exists as a pure leaf module; test_phase_fsm.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Enum + transition table are internal constants; no external/user input this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | Illegal phase transition | low | mitigate | `transition_to` raises `ValueError` on any transition absent from `LEGAL_TRANSITIONS`, preventing contradictory/skipped phase states (success criterion #4) — a correctness guard against internal logic bugs, not an adversarial boundary. |
</threat_model>

<verification>
- `python -m pytest tests/test_phase_fsm.py -x -q` passes.
- `python -m pytest tests/test_boundary.py -x -q` passes.
- `python -m pytest tests/ -q` shows no regressions.
</verification>

<success_criteria>
- Explicit, legal-only 8-state flight-phase FSM exists (PERF-02), matching roadmap success-criterion #4's named phases exactly.
- No ILS-capture sub-states and no REMOVED phase (phase-boundary respected).
</success_criteria>

## Artifacts this phase produces (introduced by this plan)

- `src/atc_sim/sim/phase.py`
- `Phase` (Enum: TAXI_OUT, DEPARTURE_ROLL, CLIMB, ENROUTE, DESCENT, APPROACH, LANDED, TAXI_IN)
- `LEGAL_TRANSITIONS` (dict), `transition_to` (function)

<output>
Create `.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-03-SUMMARY.md` when done
</output>
