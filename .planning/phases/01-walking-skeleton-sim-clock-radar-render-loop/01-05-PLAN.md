---
phase: 01-walking-skeleton-sim-clock-radar-render-loop
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - README.md
autonomous: false
requirements: [CORE-01, RADAR-01, RADAR-03]
user_setup: []

must_haves:
  truths:
    - "README Installation section documents venv creation, pip bootstrap (ensurepip fallback), editable dependency install, and the launch command — the 'not available yet' placeholder is fully removed (success criterion #5)"
    - "Following the README steps verbatim from a clean checkout launches the walking-skeleton window"
    - "The aircraft glides smoothly (no per-tick teleport) and does not streak across the canvas when it wraps at the edge (interpolation + wrap-skip working end-to-end)"
    - "Changing the render FPS cap does not change the aircraft's on-screen speed — proving CORE-01 sim/render decoupling visually"
    - "Range rings + sector lines render as a static dark-flat background; the aircraft shows a cyan dot, heading vector, and trail (RADAR-01, RADAR-03)"
  artifacts:
    - README.md
  key_links:
    - "The README launch command matches the [project.scripts] atc-sim entry point (and the python -m atc_sim.app fallback) defined in pyproject.toml by plan 01-01"
---

<objective>
Rewrite README.md's Installation section with real, verified setup and run instructions for the walking skeleton, then run the assembled application end-to-end to confirm the phase's five success criteria hold visually and behaviorally — the walking-skeleton capstone.

Purpose: Satisfy success criterion #5 (README) with instructions proven to actually launch a working app, and close the phase with a human confirmation that the sim/render decoupling is real (visible smooth motion, FPS-independent speed, graceful stall behavior).
Output: An updated README.md and a signed-off visual/behavioral verification.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-SKELETON.md
@README.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README Installation section with real setup + run instructions</name>
  <read_first>
    - README.md (current state — locate the "not available yet" placeholder to replace)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 82-104 Installation + pip/ensurepip note; lines 529-554 Environment Availability)
    - pyproject.toml (the atc-sim script entry point + dependency ranges)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-SKELETON.md (Stack Touched — the documented full-stack run path)
  </read_first>
  <files>README.md</files>
  <action>
    Replace the README Installation section's "not available yet" placeholder with concrete, ordered steps that mirror what plan 01-01 actually did and RESEARCH.md lines 82-104 prescribe: (1) prerequisites — Python 3.12+; (2) create + activate a venv (`python3 -m venv .venv`; show both `source .venv/bin/activate` and the Windows `.venv\Scripts\activate` form); (3) the pip bootstrap note — if `python -m pip --version` fails inside the venv, run `python -m ensurepip --upgrade` (RESEARCH.md Open Questions #1); (4) install: `python -m pip install --upgrade pip` then `python -m pip install -e ".[dev]"`; (5) launch the walking skeleton via the `atc-sim` console script (from pyproject `[project.scripts]`), with `python -m atc_sim.app` documented as the equivalent fallback; (6) how to run tests: `python -m pytest`. Also add a one-line description of what the running skeleton shows (a single aircraft gliding across a radar canvas with range rings, sector lines, heading vector, and trail). Do not leave any "not available"/"coming soon" placeholder text anywhere in the Installation section.
  </action>
  <verify>
    <automated>python -c "t=open('README.md').read().lower(); assert 'not available' not in t and 'coming soon' not in t, 'placeholder still present'; assert '.venv' in t and 'pip install -e' in t and 'atc-sim' in t and 'pytest' in t, 'missing required install/run step'; print('README install section OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The literal "not available" / "coming soon" placeholder text is gone from README
    - README contains venv creation, the ensurepip fallback note, `pip install -e ".[dev]"`, the `atc-sim` launch command, and the `python -m pytest` test command
    - The launch command matches pyproject.toml's `[project.scripts]` entry (`atc-sim`)
  </acceptance_criteria>
  <done>README Installation section contains real, ordered, working setup/run/test instructions with no placeholder remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): Walking-skeleton run-through sign-off</name>
  <action>Pause for the human to run the assembled walking skeleton per the README and confirm the phase success criteria (smooth motion, no wrap streak, fps-independent speed, correct radar visuals) before marking the phase complete.</action>
  <what-built>The complete walking skeleton: a fixed-timestep 2Hz sim clock (SimClock) drives one hardcoded aircraft, fully decoupled from the 60fps render loop, rendered on a bare dark-flat radar canvas with range rings, sector lines, an aircraft dot, a heading vector, and a fading trail. README now documents how to run it.</what-built>
  <how-to-verify>
    1. From a clean shell, follow the README Installation steps verbatim (create venv, install, launch via `atc-sim`). Confirm a 1280x800 window titled "ATC Simulator — Walking Skeleton (Phase 1)" opens.
    2. Confirm the static background: 4 concentric range rings + 8 sector lines radiating from center, on a dark grey/blue (18,24,32) background (RADAR-01, D-01 style).
    3. Confirm the aircraft: a cyan dot with a near-white heading vector line and a trail of fading dots behind it (RADAR-03).
    4. Confirm SMOOTH motion — the dot glides, it does NOT teleport in visible ~500ms hops (interpolation working). Watch it cross the right edge and reappear on the left: it must NOT streak across the whole canvas at the wrap (wrap-skip working).
    5. FPS-independence check (CORE-01): temporarily change `FPS_CAP` in `render/window.py` (e.g. to 30, then 120), relaunch, and confirm the aircraft crosses the canvas in the SAME wall-clock time at each fps — render frame rate must not change aircraft speed.
    6. (Optional graceful-stall spot check, CORE-02): drag the window or insert a one-time `time.sleep(1.5)` in the loop and confirm the sim catches up in bounded slow-motion rather than freezing or leaping to a far-future position (the headless test_clock.py already proves this automatically).
    7. Confirm `python -m pytest` reports all tests green.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe what looked wrong (teleporting, streak at wrap, speed changing with fps, missing rings/vector/trail).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation edit + a manual run of the local offline app; no new external input or trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | (inherited) dependency supply chain | high | mitigate | Covered by plan 01-01's blocking-human legitimacy checkpoint; the README instructs installing the same already-vetted pinned ranges. |

No STRIDE-relevant surface is introduced by this plan.
</threat_model>

<verification>
- `python -m pytest` (full suite) is green
- The README-documented steps launch the app from a clean checkout
- Human sign-off confirms smooth motion, no wrap streak, fps-independent speed, and the rings/sector-lines/dot/vector/trail visuals (phase success criteria 1-4); criterion 5 is the README rewrite itself
</verification>

<success_criteria>
README documents real working setup/run instructions (criterion #5) and a human has confirmed the walking skeleton runs with frame-rate-independent, smooth, non-streaking aircraft motion on a correct radar canvas (criteria 1-4).
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-05-SUMMARY.md` when done
</output>
