---
phase: 02-navdata-coordinate-projection
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified: []
autonomous: false
requirements: [RADAR-04, NAV-01, NAV-02]
must_haves:
  truths:
    - "A human confirms the real EGGW runway 25, OLNEY 2B / DET 2A fixes+names+tracks, and true-circle range rings all render correctly and are positioned by lat/lon"
  artifacts: []
  key_links:
    - "This capstone is the human sign-off for the visual success criteria (#1, #2, #3) that headless tests can only partially prove"
---

<objective>
Visual human-verify capstone for Phase 2. The automated tests prove the projection is circular, the data is correct, and the drawing code runs headlessly — but only a human looking at the running app can confirm the real EGGW geography is positioned correctly by lat/lon and looks right. This mirrors Phase 1's 01-05 human-verify capstone.

Purpose: Sign off the visual success criteria (#1 runway, #2 fixes/tracks, #3 true-circle rings) before phase verification.
Output: Human confirmation (no code changes).
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md
@.planning/phases/02-navdata-coordinate-projection/02-CONTEXT.md
</context>

## Phase Goal

**As a** controller, **I want to** see EGGW's real runway 25, one SID, and one STAR laid out on the radar at their true geographic positions, **so that** I can work traffic against the same real-world geometry the sim will later use for separation.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visually confirm the real EGGW radar display</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 success criteria #1-#4)
    - .planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md ("Scale & Range Selection" — expected on-screen offsets: BNN ≈ (-54,+71)px, OLNEY ≈ (-109,-122)px, DET ≈ (+286,+274)px from center at PX_PER_NM=8.0)
  </read_first>
  <what-built>The full Phase 2 radar: real EGGW runway 25 threshold + extended centerline, OLNEY 2B SID and DET 2A STAR fixes (marker + 5-letter name) connected by thin procedure tracks, all drawn through the shared cosine-corrected projection onto Phase 1's range rings; the placeholder aircraft now flies a non-wrapping straight line from the radar center.</what-built>
  <how-to-verify>
    1. Run the app: `atc-sim` (or `python -m atc_sim.app`).
    2. Runway (criterion #1): confirm a runway threshold symbol + a thin extended centerline appear near the radar center (the threshold is the projection origin), oriented roughly WSW (~254°) consistent with runway 25.
    3. Fixes/tracks (criterion #2): confirm named fixes appear — BNN, HEN, OLNEY (the SID, up/left of center) and DET, LOFFO, ABBOT (the STAR, DET far to the lower-right ~286px right / ~274px down from center) — each labelled with its 5-letter name and connected in sequence by a thin track line. No altitude/speed text is expected on-canvas (deferred to Phase 4 per D-05).
    4. Circles not ellipses (criterion #3): confirm the range rings are true circles, and the fixes sit at plausible bearings/distances (e.g. DET is clearly farther out than BNN), not squashed into an ellipse.
    5. Aircraft: confirm the dot moves smoothly in a straight line from center and does not teleport/wrap at the canvas edge (it may fly off-canvas — expected for the Phase-1 placeholder).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms runway 25 threshold + extended centerline render at a lat/lon-correct central position.
    - Human confirms all six named fixes (BNN, HEN, OLNEY, DET, LOFFO, ABBOT) appear with names and connecting thin tracks at plausible real-world positions.
    - Human confirms range rings are true circles (not ellipses) and no restriction text is drawn.
    - Human confirms the aircraft moves smoothly without wrap/teleport.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the radar renders the real EGGW geography correctly, or describe what looks wrong (position, orientation, missing element).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Verification-only plan — no new code, no inputs, no installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | n/a | Human visual verification only | low | accept | No code or dependency changes in this plan; nothing to mitigate |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan |
</threat_model>

<verification>
- `pytest` full suite green (pre-condition before the human check).
- Human sign-off recorded per the acceptance criteria above.
</verification>

<success_criteria>
A human has confirmed the running app satisfies Phase 2 visual success criteria #1 (runway), #2 (fixes/tracks), and #3 (true-circle range rings).
</success_criteria>

## Artifacts this phase produces (Plan 05)

- None (verification-only capstone; no files modified).

<output>
Create `.planning/phases/02-navdata-coordinate-projection/02-05-SUMMARY.md` when done.
</output>
