---
phase: 01-walking-skeleton-sim-clock-radar-render-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - src/atc_sim/__init__.py
  - src/atc_sim/sim/__init__.py
  - src/atc_sim/render/__init__.py
  - tests/test_boundary.py
autonomous: false
requirements: [CORE-03]
user_setup: []

must_haves:
  truths:
    - "Running `pytest` inside the project venv executes with zero collection errors"
    - "No module under src/atc_sim/sim imports the pygame library (headless boundary guard passes) — the enforcement of CORE-03's headless sim boundary (D-CORE-03)"
    - "`pip show pygame-ce pydantic pytest` reports all three installed inside the venv"
    - "`python -c 'import atc_sim'` succeeds after editable install"
  artifacts:
    - pyproject.toml
    - src/atc_sim/__init__.py
    - src/atc_sim/sim/__init__.py
    - src/atc_sim/render/__init__.py
    - tests/test_boundary.py
  key_links:
    - "pyproject [tool.setuptools.packages.find] where=['src'] resolves the atc_sim import path so editable install exposes both the package and the atc-sim script"
  prohibitions:
    - statement: "No module under src/atc_sim/sim imports pygame"
      status: enforced
      verification: "tests/test_boundary.py greps src/atc_sim/sim recursively for the pygame import statement and asserts zero matches"
---

<objective>
Stand up the greenfield project: src-layout package skeleton, `pyproject.toml` with pinned dependency ranges and pytest config, a working venv with dependencies installed, and the sim/render architectural boundary guard test that enforces CORE-03's "sim core stays headless" rule from day one.

Purpose: Establish the tooling and the single most important project-wide architectural boundary (`sim/` never imports pygame) before any sim or render code is written, so every later plan builds on a proven, testable foundation.
Output: A pip-installable package skeleton, a green (empty) test suite, and a standing boundary guard.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-SKELETON.md
</context>

<!-- planner-discipline-allow: import pygame -->

## Artifacts this phase produces (Phase 1, all plans)

New symbols/paths created across Phase 1 — excluded from source-drift verification because they are net-new:
`pyproject.toml`, `.gitignore`, `src/atc_sim/__init__.py`, `src/atc_sim/app.py`, `main()`, `src/atc_sim/sim/__init__.py`, `src/atc_sim/sim/clock.py`, `SimClock`, `SimClock.advance`, `SIM_HZ`, `TICK_DT`, `MAX_FRAME_TIME`, `MAX_TICKS_PER_FRAME`, `src/atc_sim/sim/aircraft.py`, `Aircraft`, `Aircraft.spawn_default`, `sim_step`, `CANVAS_WIDTH`, `CANVAS_HEIGHT`, `src/atc_sim/sim/interpolation.py`, `AircraftSnapshot`, `capture_state`, `interpolate`, `_lerp`, `_lerp_angle_deg`, `src/atc_sim/render/__init__.py`, `src/atc_sim/render/window.py`, `src/atc_sim/render/radar.py`, `build_static_background`, `draw_aircraft`, `draw_frame`, color constants (`BG_COLOR`, `RING_COLOR`, `SECTOR_COLOR`, `AIRCRAFT_COLOR`, `TRAIL_COLOR`, `VECTOR_COLOR`), `CENTER`, `RING_STEP_PX`, `NUM_RINGS`, `NUM_SECTOR_LINES`, `TRAIL_MAX_LEN`, `AIRCRAFT_RADIUS_PX`, `VECTOR_LENGTH_PX`, `tests/test_boundary.py`, `tests/test_clock.py`, `tests/test_interpolation.py`, `tests/test_render_smoke.py`, the `atc-sim` console-script entry point.

<tasks>

<task type="auto">
  <name>Task 1: Scaffold src-layout package and pyproject.toml</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 150-176 Recommended Project Structure; lines 479-504 Minimal pyproject.toml)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-SKELETON.md (Architectural Decisions table)
    - .claude/CLAUDE.md (Technology Stack — pinned library choices)
  </read_first>
  <files>pyproject.toml, .gitignore, src/atc_sim/__init__.py, src/atc_sim/sim/__init__.py, src/atc_sim/render/__init__.py</files>
  <action>
    Create the src-layout package skeleton exactly as RESEARCH.md lines 150-176 specify: `src/atc_sim/__init__.py`, `src/atc_sim/sim/__init__.py`, `src/atc_sim/render/__init__.py` (all empty package markers), and a `tests/` directory. Do NOT create clock.py, aircraft.py, interpolation.py, window.py, radar.py, or app.py yet — those are owned by plans 01-02, 01-03, 01-04.
    Create `pyproject.toml` from RESEARCH.md lines 479-504: `[project]` name="atc-simulator", version="0.1.0", `requires-python=">=3.12"`, dependencies `pygame-ce>=2.5,<3.0` and `pydantic>=2.13,<3.0`; `[project.optional-dependencies]` dev=`["pytest>=8.0"]`; `[project.scripts]` `atc-sim = "atc_sim.app:main"`; `[build-system]` requires `setuptools>=68` with backend `setuptools.build_meta`; `[tool.setuptools.packages.find]` where=`["src"]`. Add a `[tool.pytest.ini_options]` section with `testpaths = ["tests"]` so `pytest` discovers the tests directory.
    Use version RANGES not exact pins (research could not registry-confirm exact current versions — ranges let pip's resolver pick the true current release; per RESEARCH.md Open Questions #2).
    Create `.gitignore` covering `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `*.egg-info/`, `build/`, `dist/`.
  </action>
  <verify>
    <automated>test -f pyproject.toml && test -f src/atc_sim/__init__.py && test -f src/atc_sim/sim/__init__.py && test -f src/atc_sim/render/__init__.py && python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['scripts']['atc-sim']=='atc_sim.app:main'; assert d['tool']['setuptools']['packages']['find']['where']==['src']; print('pyproject OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` parses as valid TOML and `[project.scripts].atc-sim` equals `"atc_sim.app:main"`
    - `[tool.setuptools.packages.find].where` equals `["src"]`
    - dependencies list contains a `pygame-ce` range and a `pydantic` range; dev extra contains a `pytest` range
    - The three `__init__.py` files exist and the `tests/` directory exists
    - clock.py / aircraft.py / interpolation.py / window.py / radar.py / app.py do NOT yet exist (owned by later plans)
  </acceptance_criteria>
  <done>src-layout skeleton and pyproject.toml exist with correct script entry point, package-find root, dependency ranges, and pytest config.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2 (checkpoint): Package legitimacy sign-off before install</name>
  <action>Pause for human verification of the three [SUS]-flagged dependencies against their PyPI pages and GitHub source repos before running the install. Do not proceed to the venv install task until "approved" is received.</action>
  <what-built>pyproject.toml declares three PyPI dependencies — pygame-ce, pydantic, pytest — that the next task will install into a venv. RESEARCH.md's Package Legitimacy Audit flagged all three `[SUS]`, which the researcher attributes to a tooling limitation (PyPI weekly-download telemetry was unavailable to the checker), not a genuine concern — all three are long-established, project-locked packages with verified GitHub source repos.</what-built>
  <how-to-verify>
    1. Confirm pygame-ce at https://pypi.org/project/pygame-ce/ — established fork, source github.com/pygame-community/pygame-ce.
    2. Confirm pydantic at https://pypi.org/project/pydantic/ — source github.com/pydantic/pydantic.
    3. Confirm pytest at https://pypi.org/project/pytest/ — source github.com/pytest-dev/pytest.
    4. Confirm the pinned RANGES in pyproject.toml (`pygame-ce>=2.5,<3.0`, `pydantic>=2.13,<3.0`, `pytest>=8.0`) still resolve to current releases.
    This is expected to be a fast rubber-stamp confirmation per RESEARCH.md, not a red flag.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with dependency install, or describe any package concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create venv, verify pip, install dependencies, add sim/render boundary guard test</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 82-104 Installation + version-verification note; lines 529-554 Open Questions #1 / Environment Availability; lines 71 boundary constraint)
    - pyproject.toml (created in Task 1)
  </read_first>
  <files>tests/test_boundary.py</files>
  <action>
    Create `.venv` via `python3 -m venv .venv`, activate it, then verify pip works inside the venv with `python -m pip --version`. If pip is missing (RESEARCH.md Open Questions #1 observed the SYSTEM python lacks pip), run `python -m ensurepip --upgrade` as the documented fallback before continuing. Upgrade pip (`python -m pip install --upgrade pip`), then editable-install with dev extras: `python -m pip install -e ".[dev]"`. Confirm exact resolved versions of pygame-ce and pydantic against PyPI at install time (RESEARCH.md Installation note requires this because the researched figures were WebSearch-only, tagged `[ASSUMED]`).
    Create `tests/test_boundary.py`: a pytest test that recursively scans every `*.py` file under `src/atc_sim/sim/` and asserts none of them contains a top-level pygame import statement (match the module-import form and the `from pygame` form). Implement the scan in pure Python (read each file's text, check for the import token per non-comment line) so it works cross-platform without shelling out to grep. This enforces the CORE-03 headless-sim boundary described in RESEARCH.md line 71 and is a standing guard that every later sim module must pass.
    Note: `src/atc_sim/app.py` legitimately imports pygame but lives OUTSIDE `sim/`, so it is never in scope for this scan.
  </action>
  <verify>
    <automated>python -m pip show pygame-ce pydantic pytest >/dev/null && python -c "import atc_sim, pydantic, pygame; print('imports OK')" && python -m pytest tests/test_boundary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pip show pygame-ce pydantic pytest` reports all three installed (exit 0)
    - `python -c "import atc_sim"` succeeds (editable install exposes the package)
    - `python -m pytest tests/test_boundary.py -q` passes (zero sim modules import pygame; currently the sim package is empty so it passes trivially and stays a guard for plans 01-02/01-03)
    - The boundary scan targets `src/atc_sim/sim/` only and does not flag `src/atc_sim/app.py`
  </acceptance_criteria>
  <done>venv exists with pip verified and all dependencies installed editable; `import atc_sim` works; the sim/render boundary guard test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → dev machine | Third-party packages (pygame-ce, pydantic, pytest) are downloaded and installed into the venv — the only external-code trust boundary in Phase 1. |

No other trust boundary exists this phase: the app is fully offline, single-process, single-user, with no network I/O, no untrusted file input (scenario files arrive Phase 6), and a hardcoded aircraft spawn (RESEARCH.md Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pip install of pygame-ce / pydantic / pytest | high | mitigate | Blocking-human legitimacy checkpoint (Task above) verifies each `[SUS]` package against its PyPI page + GitHub source before install; pinned version ranges resolve current releases. |
</threat_model>

<verification>
- `python -m pytest -q` runs clean (only the boundary test exists so far; zero collection errors)
- `python -c "import atc_sim"` and `python -c "import pygame, pydantic"` both succeed inside the venv
- `atc-sim` console script is registered (`pip show -f atc-simulator` lists the entry point) even though `app.py` does not exist yet — it will resolve once plan 01-04 creates `main()`
</verification>

<success_criteria>
Package installs cleanly into a venv, `import atc_sim` works, the empty test suite is green, and the sim/render boundary guard is in place and passing.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-01-SUMMARY.md` when done
</output>
