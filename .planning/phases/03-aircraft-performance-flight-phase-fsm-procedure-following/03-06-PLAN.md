---
phase: 03-aircraft-performance-flight-phase-fsm-procedure-following
plan: 06
type: execute
wave: 4
depends_on: [03-04, 03-05]
files_modified:
  - src/atc_sim/render/radar.py
  - src/atc_sim/app.py
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PROC-01]

must_haves:
  truths:
    - "app.py spawns the demo departure/arrival pair via demo_traffic instead of a single placeholder aircraft"
    - "Each active aircraft renders at its real nm position, converted to pixels via world_to_screen"
    - "A taxiing aircraft (TAXI_OUT/TAXI_IN) renders as a stationary dot at its stand position (D-05)"
    - "The demo loop runs continuously on screen: a new departure/arrival pair appears after the previous pair completes"
  artifacts:
    - src/atc_sim/render/radar.py
    - src/atc_sim/app.py
  key_links:
    - "nm -> pixel happens via the existing world_to_screen(x_nm, y_nm, CENTER, PX_PER_NM); aircraft and navdata share one coordinate space"
    - "app.py keeps per-aircraft prev/curr snapshots and trails; removal drops the aircraft's trail"
    - "No new per-type visual symbol system (D-04) and no phase-aware branching added to radar.py beyond position"
---

## Phase Goal

**As a** controller observing the radar, **I want to** actually see the type-differentiated departure and arrival fly their full lifecycles (taxi dot, climb-out, descent, land, taxi-in) and loop continuously, **so that** the autonomous traffic is verifiable on screen, not just in tests.

<objective>
Deliver on-screen observability (the visible half of PERF-01/02/03/04, PROC-01, D-04, D-05): rewire `app.py`'s main loop to drive the `demo_traffic` collection instead of a single placeholder aircraft, and extend `render/radar.py` to draw every active aircraft at its shared-nm position converted through the existing `world_to_screen`. This is the final MVP slice — it turns the headless autonomous flight built in 03-04/03-05 into a watchable demo loop.

Purpose: Success criteria #1-#5 are all visual/observable; this plan is what a human sign-off (end-of-phase) actually watches.
Output: evolved `render/radar.py` and `app.py`.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md
@src/atc_sim/app.py
@src/atc_sim/render/radar.py
@src/atc_sim/render/window.py
@src/atc_sim/sim/interpolation.py
@src/atc_sim/sim/demo_traffic.py
@src/atc_sim/navdata/geo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: radar.py — draw a collection of aircraft, converting nm to pixels</name>
  <files>src/atc_sim/render/radar.py</files>
  <read_first>
    - src/atc_sim/render/radar.py (RenderState Protocol lines 38-46; world_to_screen lines 79-88; draw_aircraft lines 164-186; draw_frame lines 189-201)
    - src/atc_sim/render/window.py (colors; CENTER/PX_PER_NM sourcing)
    - src/atc_sim/navdata/geo.py (PX_PER_NM; the nm coordinate space aircraft now live in)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("render/radar.py (evolved, D-05)" section — zero new phase-aware logic; only extend RenderState/position handling)
  </read_first>
  <action>
    Update `src/atc_sim/render/radar.py` for the nm coordinate space and multiple aircraft. Change the `RenderState` Protocol's position fields from `x`/`y` to `x_nm`/`y_nm` (heading_deg unchanged), matching the renamed AircraftSnapshot from 03-04. Do NOT add a `phase` field to the Protocol this phase (D-04: no datablock, no per-type symbol; RESEARCH says D-05 needs zero new render logic).

    Update `draw_aircraft` so it converts the incoming nm position to pixels via the existing `world_to_screen(x_nm, y_nm, CENTER, PX_PER_NM)` before drawing the dot/vector, and converts each trail point (now stored in nm) via `world_to_screen` too. Keep the existing z-order (trail -> heading vector -> dot) and the sin=x/-cos=y heading-vector convention. The aircraft dot radius, vector length, colors, and trail fade stay unchanged (no new per-type symbol — D-04).

    Update `draw_frame` to accept a COLLECTION of render items instead of a single render_state + single trail: accept an iterable of (render_state, trail) pairs, blit the cached background once, then draw each aircraft. Preserve the read-only discipline (never mutate render_state; trails owned by app.py). A taxiing aircraft needs no special case — it simply has a constant x_nm/y_nm, so it draws as a stationary dot (D-05).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q && python -c "import atc_sim.render.radar as r; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `RenderState` Protocol uses `x_nm`/`y_nm` (no `phase` field added)
    - `draw_aircraft` and trail rendering convert nm -> pixels via `world_to_screen(..., CENTER, PX_PER_NM)`
    - `draw_frame` renders an iterable of (render_state, trail) pairs and blits the background once
    - No new per-type visual symbol logic and no phase-aware branching added to radar.py
    - `python -m pytest tests/ -q` stays green; the radar import smoke check prints `ok`
  </acceptance_criteria>
  <done>The radar layer draws any number of aircraft at their real nm positions with a single unchanged symbol style; a stationary taxi dot needs no special handling.</done>
</task>

<task type="auto">
  <name>Task 2: app.py — rewire main loop to drive the demo_traffic collection</name>
  <files>src/atc_sim/app.py</files>
  <read_first>
    - src/atc_sim/app.py (current single-aircraft main loop: spawn_default line 31, on_tick prev_state/sim_step/trail lines 45-51, capture/interpolate/draw_frame lines 53-57)
    - src/atc_sim/sim/demo_traffic.py (spawn_departure, spawn_arrival, update_demo_traffic)
    - src/atc_sim/sim/interpolation.py (capture_state, interpolate — now x_nm/y_nm)
    - src/atc_sim/render/radar.py (new draw_frame collection signature, TRAIL_MAX_LEN)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("app.py (evolved)" section — call-site replacement and prev-state-before-mutation discipline to preserve)
  </read_first>
  <action>
    Rewire `src/atc_sim/app.py`'s main loop per D-02 (a call-site change, not an addition). Replace the single `aircraft = Aircraft.spawn_default()` with an `aircraft_list` seeded by an initial `demo_traffic.spawn_departure(...)` + `spawn_arrival(...)` pair (use the first rotation types). Maintain per-aircraft state keyed by aircraft identity: a `prev_snapshot` per aircraft and a trail deque (maxlen=TRAIL_MAX_LEN) per aircraft. Use dicts keyed by `id(aircraft)` (or a small wrapper) so newly spawned aircraft get fresh entries and removed aircraft have their entries dropped.

    In `on_tick(dt)`: for each aircraft currently in the list, capture its prev snapshot BEFORE mutation; then call `demo_traffic.update_demo_traffic(aircraft_list, dt)` (which steps every aircraft via sim_step and applies removal/respawn). After update, append each surviving aircraft's new nm position to its trail (once per sim tick, matching the existing discipline). For aircraft removed this tick, drop their prev-snapshot and trail entries; for aircraft newly spawned this tick, initialize their prev snapshot to their current state so interpolation has a valid previous frame.

    In the render section: for each aircraft, compute curr snapshot and `interpolate(prev, curr, alpha)`; pass the list of (interpolated_render_state, trail) pairs to the new `draw_frame`. Preserve the module invariants documented in app.py's header (only module importing both pygame and atc_sim.sim.*; position mutation only inside on_tick; trail appended once per sim tick) and update that docstring to describe the now-plural aircraft collection and the demo loop. Keep the QUIT handling and SimClock.advance/alpha flow unchanged.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q && SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy python -c "import atc_sim.app"</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` no longer calls `Aircraft.spawn_default` (removed in 03-04); it seeds `aircraft_list` via `demo_traffic.spawn_departure`/`spawn_arrival`
    - Per-aircraft prev-snapshots and trails are maintained; removed aircraft drop their entries; newly spawned aircraft get initialized entries
    - `on_tick` captures prev snapshots before mutation and calls `demo_traffic.update_demo_traffic`; trails append once per sim tick
    - The render section interpolates per aircraft and calls the collection-based `draw_frame`
    - `python -m pytest tests/ -q` passes; `import atc_sim.app` succeeds under the dummy SDL driver (no import/wiring errors)
  </acceptance_criteria>
  <done>The app drives the looping demo of type-differentiated departures and arrivals, rendering each at its real nm position with smooth interpolation.</done>
</task>

<task type="auto">
  <name>Task 3: End-of-phase visual verification of the full demo loop</name>
  <files>.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-VALIDATION.md ("Manual-Only Verifications" — the whole-loop visual review; human_verify_mode is end-of-phase)
    - .planning/ROADMAP.md (Phase 3 Success Criteria #1-#5 — the exact things to watch for)
    - README.md (documented run command from Phase 1 — how to launch the app)
  </read_first>
  <action>
    This is the phase's end-of-phase human verification (project runs human_verify_mode: end-of-phase, so this is a `<human-check>`, not a blocking checkpoint task). Confirm the automated suite is fully green first, then have a human launch the app and watch at least one full departure and one full arrival cycle plus one automatic loop restart, checking each roadmap success criterion. No code is written in this task; it records the observed result in the SUMMARY.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <human-check>
      Launch the app (documented run command from README / `python -m atc_sim.app`). Watch a full demo cycle and confirm:
      1. Aircraft of visibly different types climb/descend/turn at visibly different rates during flight (success criterion #1; at least one loop restart shows a different type in a slot — D-04 behavioral differentiation).
      2. The departure progresses end-to-end: stationary taxi dot at the stand, then a visible departure roll, then climb-out along the OLNEY 2B SID fixes/track (success criterion #2, D-05).
      3. The arrival appears airborne at the DET entry fix and flies the DET 2A STAR track toward the airport, descending continuously (no long level-then-snap segment past LOFFO) (success criterion #3, Pitfall 7).
      4. Phase transitions look continuous and legal — no aircraft teleports between phases or skips the taxi/roll/climb (or descent/approach/land/taxi-in) sequence (success criterion #4).
      5. After landing, the arrival shows a stationary taxi-in dot and is then removed; once both aircraft are gone a fresh pair spawns automatically (success criteria #4, #5, D-03).
      Report "approved" or describe any deviation.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` is fully green (all five requirement tests active and passing)
    - Human confirmation recorded in the SUMMARY that all five roadmap success criteria were observed (or deviations noted for gap closure)
  </acceptance_criteria>
  <done>Full Phase 3 demo loop verified on screen against all five roadmap success criteria; automated suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sim -> render | The render layer reads interpolated snapshots only; it must never mutate live Aircraft state (CORE-03). No external/user input this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | Render reading sim state | low | mitigate | draw_frame/draw_aircraft read frozen interpolated snapshots and never assign to them (CORE-03); tests/test_boundary.py continues to enforce that sim/ never imports pygame, keeping the boundary intact. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` fully green (all Phase 3 requirement tests active).
- `import atc_sim.app` succeeds under `SDL_VIDEODRIVER=dummy` (wiring integrity).
- `python -m pytest tests/test_boundary.py -q` passes (sim/ still headless; only app.py + render import pygame).
- Human sign-off on the visual demo loop against roadmap success criteria #1-#5.
</verification>

<success_criteria>
- The full autonomous departure/arrival demo loop is observable on screen with behavioral type differentiation and taxi-visible dots (PERF-01..04, PROC-01, D-02..D-05).
- No new per-type visual symbol system introduced (D-04); coordinate space unified (nm -> pixel via world_to_screen).
</success_criteria>

## Artifacts this phase produces (introduced by this plan)

- Evolved `render/radar.py`: `RenderState` Protocol x_nm/y_nm; collection-based `draw_frame`; nm->pixel conversion in `draw_aircraft`
- Evolved `app.py`: demo_traffic-driven main loop with per-aircraft snapshots/trails and looping

<output>
Create `.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-06-SUMMARY.md` when done
</output>
