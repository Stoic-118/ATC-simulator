---
phase: 02-navdata-coordinate-projection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: false
requirements: [RADAR-04]
must_haves:
  truths:
    - "geographiclib is importable in the project venv"
    - "geographiclib legitimacy is human-confirmed on pypi.org before install (closes the SUS audit flag)"
  artifacts:
    - "pyproject.toml lists geographiclib>=2.1,<3.0 in [project.dependencies]"
  key_links:
    - "geographiclib is the sole geodesy dependency navdata/geo.py (Plan 02) imports for the shared cosine-corrected projection (RADAR-04)"
---

<objective>
Install the geographiclib geodesy library — the enabling dependency for the shared cosine-corrected lat/lon-to-pixel projection (RADAR-04) that Plan 02 builds. Because the automated package-legitimacy audit flagged geographiclib `[SUS]` (a sandbox-network false-positive, per 02-RESEARCH.md "Package Legitimacy Audit"), a blocking human verification precedes the install.

Purpose: Close the supply-chain gate cleanly and make `from geographiclib.geodesic import Geodesic` importable before any navdata code depends on it.
Output: Updated pyproject.toml and an installed, importable geographiclib in the venv.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md
@pyproject.toml
</context>

## Phase Goal

**As a** controller, **I want to** see EGGW's real runway 25, one SID, and one STAR laid out on the radar at their true geographic positions, **so that** I can work traffic against the same real-world geometry the sim will later use for separation.

> Note: this user story was derived from the ROADMAP.md Phase 2 `**Goal:**` line (outcome form) plus PROJECT.md's player-centric core value. It is not the canonical `mvp-phase` user-story rewrite. If a canonical story is wanted in ROADMAP.md, run `/gsd mvp-phase 02`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify geographiclib legitimacy on PyPI (closes SUS audit flag)</name>
  <read_first>
    - .planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md (section "Package Legitimacy Audit" — geographiclib flagged SUS due to a sandbox `unknown-downloads` network limitation, approved-with-note; and threat T-02-SC below)
    - .claude/CLAUDE.md ("Technology Stack" — geographiclib 2.1 is the project's pre-locked geodesy choice)
  </read_first>
  <what-built>Nothing automated yet — this is the mandatory supply-chain gate before the install. The researcher's automated legitimacy check returned SUS solely because it could not reach the live download-stats API from the sandbox; the package is the project's already-locked geodesy choice.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/geographiclib/ in a browser.
    2. Confirm: latest version is 2.x (2.1 expected), the project links to the official SourceForge page (geographiclib.sourceforge.io), the license is MIT, and the release history goes back years (not a brand-new or typosquat package).
    3. Confirm the package name is exactly `geographiclib` (not a look-alike such as `geo-graphiclib` or `geographic-lib`).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the pypi.org page shows a long-established `geographiclib` package with an official SourceForge project link and MIT license.
    - Human explicitly approves proceeding to install.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to the install task, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add geographiclib to pyproject.toml and install into the venv</name>
  <read_first>
    - pyproject.toml (current [project.dependencies] block — pygame-ce and pydantic pins; match their `>=x,<y` range style)
    - .planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md (section "Standard Stack" — pin `geographiclib>=2.1,<3.0`, pure-Python, zero native deps)
  </read_first>
  <action>Add the exact dependency specifier `geographiclib>=2.1,<3.0` to the `[project.dependencies]` array in pyproject.toml, alongside the existing `pygame-ce>=2.5,<3.0` and `pydantic>=2.13,<3.0` entries (same version-range style, per the STATE.md Phase-1 decision to use ranges not exact pins). Then install it into the active project venv with `pip install "geographiclib>=2.1,<3.0"` (or `pip install -e .` to resolve from pyproject). Do NOT hand-roll any geodesy math — this dependency exists precisely so haversine/flat-earth code is never written (02-RESEARCH.md "Don't Hand-Roll").</action>
  <verify>
    <automated>python -c "import geographiclib; from geographiclib.geodesic import Geodesic; print(geographiclib.__version__); Geodesic.WGS84.Inverse(51.877044, -0.354486, 51.726111, -0.549722)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'geographiclib>=2.1,<3.0' pyproject.toml` returns a line inside the `[project.dependencies]` array.
    - `python -c "import geographiclib; print(geographiclib.__version__)"` prints a 2.x version with no ImportError.
    - `Geodesic.WGS84.Inverse(...)` runs without error (confirms the geodesic solver is functional, not just importable).
  </acceptance_criteria>
  <done>geographiclib 2.x is pinned in pyproject.toml and importable/functional in the venv; Plan 02's geo.py can import it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local venv | A third-party package is fetched and executed in the dev/runtime environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | pip install geographiclib | high | mitigate | Blocking human verification on pypi.org (Task 1) confirms the legitimate, long-established `geographiclib` package (MIT, official SourceForge, exact name match) before install; SUS audit flag was a sandbox `unknown-downloads` false-positive, not a real signal |
</threat_model>

<verification>
- `pytest` (full suite) still green after the dependency addition — adding geographiclib must not break any Phase 1 test.
- geographiclib importable and its WGS84 geodesic solver runnable.
</verification>

<success_criteria>
geographiclib>=2.1,<3.0 is declared in pyproject.toml and installed/importable in the venv, with its legitimacy human-confirmed before install.
</success_criteria>

## Artifacts this phase produces (Plan 01)

- Modified file: `pyproject.toml` — adds `geographiclib>=2.1,<3.0` to `[project.dependencies]`.
- No new Python symbols in this plan (dependency setup only).

<output>
Create `.planning/phases/02-navdata-coordinate-projection/02-01-SUMMARY.md` when done.
</output>
