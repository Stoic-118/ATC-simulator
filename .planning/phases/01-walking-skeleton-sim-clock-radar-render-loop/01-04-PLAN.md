---
phase: 01-walking-skeleton-sim-clock-radar-render-loop
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/atc_sim/render/window.py
  - src/atc_sim/render/radar.py
  - src/atc_sim/app.py
  - tests/test_render_smoke.py
autonomous: true
requirements: [RADAR-01, RADAR-03, CORE-03]
user_setup: []

must_haves:
  truths:
    - "The radar canvas shows 4 concentric range rings and 8 sector lines radiating from center (640,400) as a static, pre-rendered background layer (RADAR-01)"
    - "The aircraft renders as a cyan dot with a near-white heading vector line and a fading trail of past sim-tick positions, drawn dot-last so it is never occluded by its own trail (RADAR-03)"
    - "Render functions receive a frozen AircraftSnapshot and never mutate sim state — attempting to write a frozen snapshot raises (CORE-03 read-only render path)"
    - "The main loop appends a trail point once per sim tick (inside on_tick), never once per render frame, so the trail is true ~4s sim-tick history not a dense render-frame cloud"
    - "A headless (SDL_VIDEODRIVER=dummy) render smoke test builds the static background and draws a full frame without raising"
    - "Colors match D-01 modern-flat palette: bg (18,24,32), aircraft cyan (0,220,220), vector near-white (230,240,240)"
  artifacts:
    - src/atc_sim/render/window.py
    - src/atc_sim/render/radar.py
    - src/atc_sim/app.py
    - tests/test_render_smoke.py
  key_links:
    - "app.py is the only module importing BOTH pygame and sim/*; it advances the SimClock each frame, captures prev/curr snapshots, interpolates, and passes the interpolated snapshot (never the live Aircraft) to draw_frame"
    - "build_static_background is called once at startup and blitted each frame; only the aircraft dot/vector/trail redraw per frame"
  prohibitions:
    - statement: "render/*.py never invokes the aircraft mutator or mutates an Aircraft"
      status: enforced
      verification: "render draw functions accept only a frozen AircraftSnapshot (mutation raises); render smoke test asserts no exception drawing from a frozen snapshot"
---

<!-- planner-discipline-allow: sim_step -->

<objective>
Build the render layer (window/constants, cached radar background, aircraft dot/vector/trail) and wire it to the sim core in `app.py`'s main loop — producing the first visible, running walking skeleton: one aircraft gliding across a bare radar canvas, its motion driven only by sim-tick state.

Purpose: Deliver the user-visible half of the walking skeleton and the composition-root seam between the two architecture halves, satisfying RADAR-01 (rings/sector lines), RADAR-03 (dot/vector/trail), and CORE-03's read-only render path.
Output: `render/window.py`, `render/radar.py`, `app.py`, and a headless render smoke test.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Window setup + cached radar background (rings + sector lines)</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 362-402 radar background code + color constants)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-UI-SPEC.md (Radar Canvas Geometry table; Color table; Rendering Behavior Notes)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md (lines 168-179, 209-219)
  </read_first>
  <files>src/atc_sim/render/window.py, src/atc_sim/render/radar.py</files>
  <action>
    Create `src/atc_sim/render/window.py` holding the render-side constants and window setup: `WINDOW_SIZE = (1280, 800)` (D-03 fixed), `FPS_CAP = 60`, `WINDOW_CAPTION = "ATC Simulator — Walking Skeleton (Phase 1)"` (UI-SPEC Copywriting), and the D-01 color constants from RESEARCH.md lines 370-376 (`BG_COLOR=(18,24,32)`, `RING_COLOR=(60,90,100)`, `SECTOR_COLOR=(40,60,68)`, `AIRCRAFT_COLOR=(0,220,220)`, `TRAIL_COLOR=(0,140,140)`, `VECTOR_COLOR=(230,240,240)`). Provide a small `create_window()` helper that runs `pygame.display.set_mode(WINDOW_SIZE)` and `pygame.display.set_caption(WINDOW_CAPTION)` and returns the screen surface.
    Create `src/atc_sim/render/radar.py` with `build_static_background(size)` copied from RESEARCH.md lines 383-400: fill BG_COLOR, draw `NUM_RINGS = 4` concentric rings at `RING_STEP_PX = 80` spacing (`pygame.draw.circle(..., width=1)`) centered at `CENTER = (640, 400)`, and `NUM_SECTOR_LINES = 8` anti-aliased sector lines (`pygame.draw.aaline`) radiating from CENTER to the outer ring using the compass convention (`end = (CX + sin(angle)*R, CY - cos(angle)*R)`). Return the pre-rendered Surface (built once, blitted each frame — do not redraw primitives per frame). Import color constants from window.py so the palette lives in one place.
  </action>
  <verify>
    <automated>SDL_VIDEODRIVER=dummy python -c "import pygame; pygame.init(); from atc_sim.render.radar import build_static_background, CENTER, NUM_RINGS, NUM_SECTOR_LINES; s=build_static_background((1280,800)); assert s.get_size()==(1280,800); assert CENTER==(640,400) and NUM_RINGS==4 and NUM_SECTOR_LINES==8; print('background OK'); pygame.quit()"</automated>
  </verify>
  <acceptance_criteria>
    - `build_static_background((1280,800))` returns a 1280x800 Surface with no exception under SDL dummy driver
    - `CENTER == (640, 400)`, `NUM_RINGS == 4`, `NUM_SECTOR_LINES == 8`, `RING_STEP_PX == 80`
    - Color constants equal the D-01 values (bg (18,24,32), rings (60,90,100), sector (40,60,68))
    - `WINDOW_CAPTION` is the UI-SPEC caption string
  </acceptance_criteria>
  <done>The static radar background (4 rings + 8 sector lines) renders to a cached surface with the D-01 palette; window constants/setup exist.</done>
</task>

<task type="auto">
  <name>Task 2: Aircraft symbol — trail, heading vector, dot</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 404-433 draw_aircraft + trail structure note)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-UI-SPEC.md (Radar Canvas Geometry: dot radius 5, vector 40px, trail deque(8); Rendering Behavior Notes: dot draws last, heading convention)
    - src/atc_sim/render/radar.py (from Task 1)
    - src/atc_sim/sim/interpolation.py (AircraftSnapshot shape)
  </read_first>
  <files>src/atc_sim/render/radar.py</files>
  <action>
    Extend `render/radar.py` with the aircraft-drawing constants and functions from RESEARCH.md lines 404-433: `TRAIL_MAX_LEN = 8`, `AIRCRAFT_RADIUS_PX = 5`, `VECTOR_LENGTH_PX = 40`. Add `draw_aircraft(surface, x, y, heading_deg, trail)` that draws in this exact z-order: (1) trail dots from the passed `deque` of past positions, oldest fainter/smaller via age-based fade (`radius = max(1, int(AIRCRAFT_RADIUS_PX*0.4*fade))`); (2) the heading vector as `pygame.draw.aaline` from (x,y) to `(x + sin(rad)*VECTOR_LENGTH_PX, y - cos(rad)*VECTOR_LENGTH_PX)` using compass convention (0deg=up/north, clockwise — UI-SPEC Rendering Behavior Notes); (3) the aircraft dot LAST (`pygame.draw.circle`, AIRCRAFT_RADIUS_PX) so it sits above its own trail/vector.
    Add `draw_frame(screen, background, render_state, trail)` that blits the cached `background` then calls `draw_aircraft` with `render_state.x`, `render_state.y`, `render_state.heading_deg`. `render_state` is a frozen `AircraftSnapshot` — draw functions read it only, never assign to it (CORE-03). The `trail` is owned/updated by app.py (appended once per sim tick), not by the render layer.
  </action>
  <verify>
    <automated>SDL_VIDEODRIVER=dummy python -c "import pygame; from collections import deque; pygame.init(); from atc_sim.render.radar import build_static_background, draw_frame, TRAIL_MAX_LEN, AIRCRAFT_RADIUS_PX, VECTOR_LENGTH_PX; from atc_sim.sim.interpolation import AircraftSnapshot; scr=pygame.display.set_mode((1280,800)); bg=build_static_background((1280,800)); snap=AircraftSnapshot(x=640.0,y=400.0,heading_deg=45.0); t=deque([(600.0,380.0),(620.0,390.0)], maxlen=TRAIL_MAX_LEN); draw_frame(scr,bg,snap,t); assert (AIRCRAFT_RADIUS_PX,VECTOR_LENGTH_PX,TRAIL_MAX_LEN)==(5,40,8); print('aircraft draw OK'); pygame.quit()"</automated>
  </verify>
  <acceptance_criteria>
    - `draw_frame` blits the background and draws the aircraft from a frozen `AircraftSnapshot` without raising (SDL dummy)
    - z-order is trail → vector → dot (dot drawn last)
    - `AIRCRAFT_RADIUS_PX == 5`, `VECTOR_LENGTH_PX == 40`, `TRAIL_MAX_LEN == 8`
    - Heading vector uses compass convention (`sin` for x, `-cos` for y)
  </acceptance_criteria>
  <done>The aircraft dot, heading vector, and fading trail render from an interpolated frozen snapshot with correct z-order and D-01 accent colors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire app.py main loop + headless render smoke test</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 235-272 app.py main loop; lines 321-325 anti-patterns; lines 430-433 trail-per-tick rule)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md (lines 130-166 app.py source)
    - src/atc_sim/sim/clock.py, src/atc_sim/sim/aircraft.py, src/atc_sim/sim/interpolation.py, src/atc_sim/render/radar.py, src/atc_sim/render/window.py
  </read_first>
  <files>src/atc_sim/app.py, tests/test_render_smoke.py</files>
  <action>
    Create `src/atc_sim/app.py` with `main()` from RESEARCH.md lines 243-271, adapted to use `render/window.py`'s `create_window()` and constants and `render/radar.py`'s `build_static_background`/`draw_frame`: run `pygame.init()`; create the window; `pg_clock = pygame.time.Clock()`; `sim_clock = SimClock()`; `aircraft = Aircraft.spawn_default()`; build the static background once; create a `trail = collections.deque(maxlen=TRAIL_MAX_LEN)`; capture the initial `prev_state`. Main `while running:` loop: pump events (QUIT ends the loop); `real_dt = pg_clock.tick(FPS_CAP) / 1000.0`; define `on_tick(dt)` that sets `prev_state = capture_state(aircraft)`, advances state via the aircraft mutator for one tick, then appends the aircraft's post-tick `(x, y)` to `trail` — the trail is appended ONCE PER SIM TICK inside on_tick, NEVER once per render frame (RESEARCH.md lines 430-433, load-bearing); `alpha = sim_clock.advance(real_dt, on_tick)`; `curr_state = capture_state(aircraft)`; `render_state = interpolate(prev_state, curr_state, alpha)`; `draw_frame(screen, background, render_state, trail)`; `pygame.display.flip()`. Call `pygame.quit()` on exit. All position mutation happens inside on_tick only — never in the render section using frame dt (RESEARCH.md anti-pattern lines 321-325).
    Create `tests/test_render_smoke.py`: set the SDL dummy video driver via `os.environ["SDL_VIDEODRIVER"] = "dummy"` at the top of the file BEFORE importing pygame, then `pygame.init()`, build the background, construct an `AircraftSnapshot`, call `draw_frame` with a small trail deque, and assert it completes without raising. This is the automated Nyquist guard for the otherwise-visual RADAR-01/RADAR-03 (full visual correctness is the human-verify capstone in plan 01-05).
  </action>
  <verify>
    <automated>SDL_VIDEODRIVER=dummy python -m pytest tests/test_render_smoke.py tests/test_boundary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `SDL_VIDEODRIVER=dummy python -m pytest tests/test_render_smoke.py -q` passes (frame drawn without exception)
    - `python -m pytest tests/test_boundary.py -q` still passes (no sim module imports pygame; app.py is outside sim/ so its pygame import is allowed)
    - `app.py` defines `main()` and updates the trail deque inside the `on_tick` callback, not in the per-frame render section
    - `app.py` is the only module importing both pygame and `atc_sim.sim.*`
  </acceptance_criteria>
  <done>The main loop runs the accumulator, interpolates, and draws each frame; the headless render smoke test and the boundary guard both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Rendering reads in-memory sim snapshots and draws to a local window; no network, no file input, no untrusted data (RESEARCH.md Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | (inherited) dependency supply chain | high | mitigate | Covered by plan 01-01's blocking-human legitimacy checkpoint; no new packages here. |

No STRIDE-relevant surface is introduced: the render layer is a read-only consumer of local sim state.
</threat_model>

<verification>
- `SDL_VIDEODRIVER=dummy python -m pytest -q` runs the full suite (clock + interpolation + boundary + render smoke) green
- The running app (visual) is deferred to the human-verify capstone in plan 01-05; this plan proves it constructs and draws headlessly without error
</verification>

<success_criteria>
The radar background (rings + sector lines), the aircraft dot/heading-vector/trail, and the full sim→interpolate→draw main loop are implemented and pass headless smoke + boundary tests, satisfying RADAR-01, RADAR-03, and CORE-03's read-only render path.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-04-SUMMARY.md` when done
</output>
