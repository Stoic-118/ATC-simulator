---
phase: 01-walking-skeleton-sim-clock-radar-render-loop
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/atc_sim/sim/aircraft.py
  - src/atc_sim/sim/interpolation.py
  - tests/test_interpolation.py
autonomous: true
requirements: [CORE-03, RADAR-03]
user_setup: []

must_haves:
  truths:
    - "interpolate() leaves both input AircraftSnapshot instances unchanged (frozen=True enforced) — the render read path never mutates sim state (CORE-03)"
    - "Heading interpolation from 350deg to 10deg passes through 0/360, never backward through 180deg — shortest-path angle lerp for the heading vector (RADAR-03)"
    - "A position jump greater than half the canvas dimension between snapshots snaps to the current snapshot instead of lerping, so the D-02 wrap-at-edge is not drawn as a full-canvas streak"
    - "sim_step advances the aircraft along heading_deg using compass convention (0deg=north/up, clockwise) and wraps position at canvas edges (D-02)"
    - "Aircraft rejects an out-of-range heading (>=360 or <0) and a non-positive speed at construction (Field bounds)"
  artifacts:
    - src/atc_sim/sim/aircraft.py
    - src/atc_sim/sim/interpolation.py
    - tests/test_interpolation.py
  key_links:
    - "AircraftSnapshot mirrors exactly the interpolated fields (x, y, heading_deg); capture_state reads a live Aircraft and returns a frozen snapshot the renderer can hold safely"
  prohibitions:
    - statement: "sim/aircraft.py and sim/interpolation.py do not import pygame"
      status: enforced
      verification: "tests/test_boundary.py (from plan 01-01) scans all sim modules and asserts zero pygame imports"
---

<objective>
Implement the authoritative `Aircraft` state model with its Phase-1 straight-line-then-wrap motion (D-02), and the pure snapshot/interpolation math that lets the 2Hz sim state render smoothly at 60fps — test-first for the two easy-to-get-subtly-wrong pieces (shortest-path heading lerp and the wrap-skip edge case).

Purpose: Provide the sim-authoritative state and the read-only interpolation seam. Interpolation reading two frozen snapshots and returning a third throwaway value is exactly how CORE-03 ("render reads state, never mutates it") is satisfied cleanly, and the shortest-path heading lerp is what makes RADAR-03's heading vector correct across the 0/360 boundary.
Output: `sim/aircraft.py`, `sim/interpolation.py`, and a green `tests/test_interpolation.py`.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for interpolation math and wrap-skip</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 274-319 Pattern 2 + wrap edge-case note; lines 570-578 test map for CORE-03/RADAR-03)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md (lines 103-128 interpolation source + edge-case; lines 184-189 required tests)
  </read_first>
  <files>tests/test_interpolation.py</files>
  <behavior>
    - test_interpolate_does_not_mutate_inputs: build two AircraftSnapshot instances, call interpolate(prev, curr, 0.5), assert both inputs are byte-for-byte unchanged afterward (frozen model prevents mutation; also assert the returned object is a distinct third instance).
    - test_position_lerp_midpoint: interpolate(x=0->100, y=200->200, heading same, alpha=0.5) yields x≈50, y≈200.
    - test_heading_interpolation_shortest_path: interpolate heading 350deg -> 10deg at alpha=0.5 yields ≈0deg (i.e. within a small epsilon of 0/360), NOT ≈180deg; also test 10deg -> 350deg goes backward through 0, and a normal 90deg -> 100deg at 0.5 yields ≈95deg.
    - test_wrap_skip_snaps_to_current: when abs(curr.x - prev.x) > CANVAS_WIDTH/2 (simulating the D-02 wrap), interpolate returns the CURRENT snapshot's x (snap), not a value lerped across the seam. Same behavior for a large y jump.
    - test_sim_step_moves_along_heading: an Aircraft at heading_deg=90 (east) after sim_step(dt=0.5) has increased x and unchanged-ish y (compass convention: x += sin(rad)*speed*dt, y -= cos(rad)*speed*dt).
    - test_sim_step_wraps_at_edge: an Aircraft near the right edge steps past CANVAS_WIDTH and wraps to a small x.
    - test_aircraft_rejects_bad_fields: constructing Aircraft with heading_deg=360 or speed_px_per_sec=0 raises a pydantic ValidationError.
  </behavior>
  <action>
    Write `tests/test_interpolation.py` importing `Aircraft`, `sim_step`, `CANVAS_WIDTH`, `CANVAS_HEIGHT` from `atc_sim.sim.aircraft`, and `AircraftSnapshot`, `capture_state`, `interpolate` from `atc_sim.sim.interpolation`. Encode the seven behaviors above. Use `pytest.raises` (import pydantic.ValidationError) for the bad-fields case, and `pytest.approx` for float comparisons. These tests MUST fail now because neither sim module exists yet (import error = expected RED). Do not implement the modules in this task.
  </action>
  <verify>
    <automated>python -m pytest tests/test_interpolation.py -q; test $? -ne 0 && echo "RED confirmed (tests fail as expected — sim modules not implemented)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_interpolation.py` defines the seven named test functions from the behavior block
    - Running the file fails (RED) with an ImportError for the not-yet-written sim modules
    - Heading-boundary and wrap-skip cases assert the shortest-path / snap behavior explicitly (not just "runs without error")
  </acceptance_criteria>
  <done>Seven failing tests fully specify aircraft motion, no-mutate interpolation, shortest-path heading, and the wrap-skip edge case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement Aircraft and interpolation to pass the tests</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 435-477 Aircraft model + sim_step; lines 284-319 interpolation module + wrap edge-case note)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md (lines 73-128)
    - tests/test_interpolation.py (created in Task 1)
  </read_first>
  <files>src/atc_sim/sim/aircraft.py, src/atc_sim/sim/interpolation.py</files>
  <action>
    Implement `src/atc_sim/sim/aircraft.py` from RESEARCH.md lines 435-477: module constants `CANVAS_WIDTH = 1280`, `CANVAS_HEIGHT = 800` (D-03 fixed window); `Aircraft(BaseModel)` with `model_config = ConfigDict(validate_assignment=True)`, fields `x: float`, `y: float`, `heading_deg: float = Field(ge=0.0, lt=360.0)`, `speed_px_per_sec: float = Field(gt=0.0)`; classmethod `spawn_default()` returning `cls(x=0.0, y=CANVAS_HEIGHT/2, heading_deg=90.0, speed_px_per_sec=60.0)` (D-02 straight-line east); and `sim_step(aircraft, dt)` mutating x/y along heading via compass convention (`x += sin(rad)*speed*dt`, `y -= cos(rad)*speed*dt`) then wrapping x within [0, CANVAS_WIDTH) and y within [0, CANVAS_HEIGHT). Use `heading_deg` (never bare `heading`) per PITFALLS.md Pitfall 3 to avoid a Phase-2 rename.
    Implement `src/atc_sim/sim/interpolation.py` from RESEARCH.md lines 284-319: `AircraftSnapshot(BaseModel)` with `model_config = ConfigDict(frozen=True)` and fields x/y/heading_deg; `capture_state(aircraft) -> AircraftSnapshot`; `_lerp(a,b,t)`; `_lerp_angle_deg(a,b,t)` using the shortest-path formula `diff = ((b - a + 180) % 360) - 180; return (a + diff*t) % 360`; and `interpolate(prev, curr, alpha)` returning a new AircraftSnapshot. Add the D-02 wrap-skip guard (RESEARCH.md lines 319, PATTERNS.md line 128): if `abs(curr.x - prev.x) > CANVAS_WIDTH/2` or `abs(curr.y - prev.y) > CANVAS_HEIGHT/2`, return `curr` (snap) instead of lerping position — do not build a general seam abstraction, this is a Phase-1-only special case removed in Phase 2.
    Neither module imports pygame (enforced by tests/test_boundary.py).
  </action>
  <verify>
    <automated>python -m pytest tests/test_interpolation.py tests/test_boundary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_interpolation.py -q` passes all seven tests (GREEN)
    - `python -m pytest tests/test_boundary.py -q` still passes (neither sim module imports pygame)
    - `_lerp_angle_deg(350, 10, 0.5)` is within epsilon of 0/360, and `interpolate` snaps to `curr` when the position jump exceeds half a canvas dimension
    - `Aircraft` uses `validate_assignment=True`; `AircraftSnapshot` uses `frozen=True`
  </acceptance_criteria>
  <done>Aircraft, sim_step, snapshot capture, and interpolation are implemented; all interpolation tests and the boundary guard pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory state model + math. The one construction point (`spawn_default`) is hardcoded; Pydantic `Field` bounds provide basic sanity validation (RESEARCH.md Security Domain V5, marginal). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | (inherited) dependency supply chain | high | mitigate | Covered by plan 01-01's blocking-human legitimacy checkpoint; no new packages here. |

No untrusted-input boundary exists in Phase 1 (scenario-file parsing is Phase 6). Pydantic field bounds are the only input validation and guard the single hardcoded spawn.
</threat_model>

<verification>
- `python -m pytest tests/test_interpolation.py tests/test_clock.py tests/test_boundary.py -q` is green
- Heading interpolation across the 0/360 seam and the wrap-skip both behave as asserted
</verification>

<success_criteria>
The authoritative aircraft state, its D-02 motion, and the read-only shortest-path interpolation seam are implemented and proven correct by headless tests, satisfying CORE-03 (no-mutate read path) and the heading-vector correctness underpinning RADAR-03.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-03-SUMMARY.md` when done
</output>
