---
phase: 03-aircraft-performance-flight-phase-fsm-procedure-following
plan: 04
type: execute
wave: 2
depends_on: [03-02, 03-03]
files_modified:
  - src/atc_sim/sim/aircraft.py
  - src/atc_sim/sim/procedure.py
  - src/atc_sim/sim/interpolation.py
autonomous: true
requirements: [PROC-01, PERF-02, PERF-03, PERF-04]

must_haves:
  truths:
    - "Aircraft stores x_nm/y_nm (shared local-nm coord space), altitude_ft, speed_kt, heading_deg (MAGNETIC), phase, phase_elapsed_sec, a procedure ref, procedure_leg_index, and a performance ref"
    - "compute_target on the DET STAR LOFFO leg (no restriction) targets ABBOT's FL080, not held at FL170 (restriction look-ahead)"
    - "compute_target's heading_deg is magnetic (passed through true_to_magnetic exactly once)"
    - "sim_step dispatches per-phase kinematics via small named helpers, transitioning phases only via transition_to when the completion condition is met"
    - "advance_leg_if_reached increments procedure_leg_index when within capture radius of the current fix"
  artifacts:
    - src/atc_sim/sim/aircraft.py
    - src/atc_sim/sim/procedure.py
    - src/atc_sim/sim/interpolation.py
  key_links:
    - "procedure.py reuses navdata/geo.py (project_to_local_xy_nm, true_to_magnetic) — never reimplements bearing/distance math"
    - "procedure.py imports Aircraft only under TYPE_CHECKING to avoid a circular import with aircraft.py"
    - "AircraftSnapshot field names mirror Aircraft's position fields (x_nm/y_nm) in lockstep"
---

## Phase Goal

**As a** controller observing the radar, **I want to** watch an aircraft autonomously turn toward its procedure fixes, climb/descend to charted restrictions, and sequence legs, **so that** it flies the SID/STAR believably with no player input.

<objective>
Deliver the autonomous-flight core (PROC-01, and the kinematics that enable PERF-02/03/04): evolve `Aircraft` into the shared local-nm coordinate system with altitude/phase/procedure/performance state; add `procedure.py`'s `compute_target()` with restriction look-ahead and once-only true->magnetic conversion; add `sim_step()`'s per-phase kinematics dispatch. Update `interpolation.py` in lockstep with the field rename. This makes `tests/test_procedure_following.py` activate and pass.

Purpose: This is the thinnest end-to-end vertical slice of autonomous flight — an `Aircraft` object can now fly a real procedure leg-by-leg toward its restrictions. The demo harness (03-05) and on-screen observability (03-06) build directly on it.
Output: evolved `aircraft.py`, new `procedure.py`, evolved `interpolation.py`.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md
@src/atc_sim/sim/aircraft.py
@src/atc_sim/sim/interpolation.py
@src/atc_sim/sim/phase.py
@src/atc_sim/sim/performance.py
@src/atc_sim/navdata/models.py
@src/atc_sim/navdata/geo.py
@src/atc_sim/navdata/eggw.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evolve Aircraft into local-nm coordinate + procedure/performance/phase state; update interpolation lockstep</name>
  <files>src/atc_sim/sim/aircraft.py, src/atc_sim/sim/interpolation.py</files>
  <read_first>
    - src/atc_sim/sim/aircraft.py (current model: x, y pixels, heading_deg, speed_px_per_sec; validate_assignment convention line 26; spawn_default; sim_step)
    - src/atc_sim/sim/interpolation.py (AircraftSnapshot mirrors Aircraft x/y/heading_deg; capture_state; interpolate)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md ("Why local-plane coordinates" lines 407; Pattern C single phase_elapsed_sec field lines 312-321)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("aircraft.py (evolved)" and "interpolation.py (evolved)" sections — conventions to preserve, field-rename lockstep rule)
    - src/atc_sim/navdata/models.py (Procedure type imported for the procedure field)
  </read_first>
  <behavior>
    - An Aircraft can be constructed with x_nm/y_nm/altitude_ft/speed_kt/heading_deg/phase/procedure/procedure_leg_index/performance
    - heading_deg still enforces Field(ge=0.0, lt=360.0); rate/speed/altitude fields enforce sensible bounds
    - AircraftSnapshot round-trips the renamed position fields; interpolate lerps them
  </behavior>
  <action>
    Evolve `src/atc_sim/sim/aircraft.py`'s `Aircraft` model, preserving `model_config = ConfigDict(validate_assignment=True)` exactly. Replace the pixel `x`/`y` fields with `x_nm: float` and `y_nm: float` (shared local tangent-plane nm coordinates, same space as navdata/geo.py project_to_local_xy_nm output — RESEARCH "Why local-plane coordinates"). Replace `speed_px_per_sec` with `speed_kt: float = Field(gt=0.0)`. Keep `heading_deg: float = Field(ge=0.0, lt=360.0)` and document with an inline `# Phase 3` comment that heading_deg is MAGNETIC (matches every navdata magnetic field; RESEARCH Pitfall "Heading stored as true bearing"). Add: `altitude_ft: float = Field(ge=0.0)`; `phase: Phase` (import from atc_sim.sim.phase); `phase_elapsed_sec: float = 0.0` (single generic timer reused across every timed phase — RESEARCH Pattern C; reset to 0.0 on each transition); `procedure: Procedure` (import from atc_sim.navdata.models); `procedure_leg_index: int = Field(default=0, ge=0)`; `performance: PerformanceProfile` (import from atc_sim.sim.performance). Do NOT add an `is_departure: bool` — the initial `phase` value alone determines the traversal (RESEARCH Pattern B "Spawn phase depends on flight kind, not a new field").

    Remove `spawn_default()` (D-02: the demo harness in 03-05 owns spawn construction; keeping a stale pixel-based classmethod would reference removed fields). Note in an inline comment that app.py's call site is rewired in plan 03-06.

    Keep the `CANVAS_WIDTH`/`CANVAS_HEIGHT` constants only if still referenced; otherwise remove them (they were pixel-space spawn helpers). Preserve the mandatory "MUST NOT import pygame" docstring sentence.

    Update `src/atc_sim/sim/interpolation.py` in lockstep: rename `AircraftSnapshot` position fields to `x_nm`/`y_nm`; update `capture_state` to read `aircraft.x_nm`/`aircraft.y_nm`; update `interpolate` to lerp `x_nm`/`y_nm` (heading_deg lerp unchanged). Do NOT add altitude_ft to the snapshot this phase (RESEARCH Open Question 1 / D-04: no datablock yet, altitude need not be interpolated). Add a `# Phase 3` inline comment explaining the rename mirrors the Aircraft coordinate-space move.
  </action>
  <verify>
    <automated>python -m pytest tests/test_boundary.py -q && python -c "from atc_sim.sim.aircraft import Aircraft; from atc_sim.sim.interpolation import capture_state, interpolate; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `Aircraft` has fields x_nm, y_nm, altitude_ft, speed_kt, heading_deg, phase, phase_elapsed_sec, procedure, procedure_leg_index, performance and keeps `validate_assignment=True`
    - No `is_departure` field exists; `spawn_default` removed
    - `AircraftSnapshot` uses x_nm/y_nm; `capture_state`/`interpolate` reference the renamed fields; no field-name divergence between Aircraft and AircraftSnapshot
    - The import smoke check prints `ok`; `python -m pytest tests/test_boundary.py -q` passes (both sim modules stay headless)
  </acceptance_criteria>
  <done>Aircraft lives in the shared nm coordinate space with full procedure/performance/phase state; interpolation renamed in lockstep; no stale pixel-space code remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: procedure.py — compute_target with restriction look-ahead + magnetic heading + leg sequencing</name>
  <files>src/atc_sim/sim/procedure.py</files>
  <read_first>
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md (Pattern D lines 333-407 — compute_target, _next_altitude_restriction_ft, _next_speed_restriction_kt, advance_leg_if_reached; and the "Restriction look-ahead omitted" + "Heading stored as true bearing" pitfalls)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("procedure.py" section — reuse geo helpers, sin=x/cos=y convention, true_to_magnetic boundary, dataclass Targets)
    - src/atc_sim/navdata/geo.py (project_to_local_xy_nm, true_to_magnetic, true_bearing_and_distance_nm — the ONLY geodesy source to call)
    - src/atc_sim/navdata/eggw.py (DET_2A_STAR leg data: DET at 17000, LOFFO no altitude restriction, ABBOT at 8000 — the look-ahead regression case)
    - src/atc_sim/navdata/models.py (ProcedureLeg / AltitudeRestriction / SpeedRestriction shapes)
    - tests/test_procedure_following.py (the assertions this module must satisfy)
  </read_first>
  <behavior>
    - compute_target on the LOFFO leg (index 1, no altitude restriction) returns altitude_ft == 8000 (look-ahead to ABBOT), not 17000
    - compute_target's heading_deg equals true_to_magnetic(<true bearing to the current fix>) within tolerance
    - advance_leg_if_reached increments procedure_leg_index only when planar distance to the current fix <= capture radius
  </behavior>
  <action>
    Create `src/atc_sim/sim/procedure.py` with the mandatory "MUST NOT import pygame" docstring sentence. Import `math` and `from atc_sim.navdata.geo import project_to_local_xy_nm, true_to_magnetic`. Import `Aircraft` ONLY under `if TYPE_CHECKING:` (add `from __future__ import annotations` at the top) so procedure.py does not create a runtime circular import with aircraft.py (aircraft.py imports procedure at runtime; procedure needs Aircraft for type hints only).

    Define `@dataclass(frozen=True) class Targets` with fields `heading_deg: float` (MAGNETIC), `altitude_ft: float`, `speed_kt: float`. Use `dataclasses`, not Pydantic (recomputed every tick; CLAUDE.md convention).

    Implement `_next_altitude_restriction_ft(aircraft)` and `_next_speed_restriction_kt(aircraft)` per RESEARCH Pattern D: scan `aircraft.procedure.legs[aircraft.procedure_leg_index:]` for the first leg whose altitude_restriction / speed_restriction is not None and return that value; the altitude fallback (no more restrictions) is `aircraft.altitude_ft`, the speed fallback is `aircraft.performance.terminal_speed_kt`. This look-ahead is exactly what makes DET STAR's unrestricted LOFFO leg produce a continuous descent toward ABBOT's FL080 instead of a level-then-snap profile (Pitfall 7).

    Implement `compute_target(aircraft) -> Targets`: get the current leg's fix, project it via `project_to_local_xy_nm(leg.fix.lat, leg.fix.lon)`, compute `dx = fix_x_nm - aircraft.x_nm`, `dy = fix_y_nm - aircraft.y_nm`, `true_bearing = math.degrees(math.atan2(dx, dy)) % 360.0` (sin=x/cos=y convention, matching aircraft.py/geo.py), then `heading_deg = true_to_magnetic(true_bearing)` — call true_to_magnetic EXACTLY ONCE (Pitfall 4). Return Targets(heading_deg, _next_altitude_restriction_ft(aircraft), _next_speed_restriction_kt(aircraft)).

    Implement `advance_leg_if_reached(aircraft, capture_radius_nm=1.0)`: project the current leg's fix to nm, compute planar `math.hypot(fix_x - aircraft.x_nm, fix_y - aircraft.y_nm)`, and if `<= capture_radius_nm` increment `aircraft.procedure_leg_index` by 1 (simplified fly-by sequencing; no per-tick geodesic call — RESEARCH performance note). Guard against advancing past the last leg (do not index out of range in later reads; leave index sequencing to demo_traffic's removal check).

    Do NOT build any localizer/glideslope/intercept-angle logic here — APPROACH targeting is handled by the simplified glidepath helper in the sim_step dispatch (Task 3), never real ILS geometry (Phase 4 / PROC-03 boundary).
  </action>
  <verify>
    <automated>python -m pytest tests/test_procedure_following.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/atc_sim/sim/procedure.py` exists with the "MUST NOT import pygame" sentence and imports Aircraft only under TYPE_CHECKING
    - `compute_target` calls `project_to_local_xy_nm` and `true_to_magnetic` (reuses geo.py; no reimplemented bearing/distance math) and calls `true_to_magnetic` exactly once
    - On the DET STAR LOFFO leg, `compute_target(...).altitude_ft == 8000`
    - `advance_leg_if_reached` increments `procedure_leg_index` when within the capture radius and does not when outside it
    - `python -m pytest tests/test_procedure_following.py -x -q` passes; `python -m pytest tests/test_boundary.py -q` passes
  </acceptance_criteria>
  <done>Procedure-following with restriction look-ahead and once-only magnetic conversion exists; test_procedure_following.py green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sim_step per-phase kinematics dispatch</name>
  <files>src/atc_sim/sim/aircraft.py</files>
  <read_first>
    - src/atc_sim/sim/aircraft.py (current sim_step signature `def sim_step(aircraft, dt) -> None` and the sin=x/-cos=y motion convention lines 44-55)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md (Code Examples lines 466-505 — sim_step dispatch sketch, _step_* helpers; Pattern C taxi/departure-roll/landing/approach semantics lines 308-331; "Per-phase kinematics dispatch scattered as inline conditionals" pitfall)
    - src/atc_sim/sim/performance.py (turn_rate_deg_per_sec, step_toward_heading, step_toward_value, glidepath_altitude_ft)
    - src/atc_sim/sim/phase.py (Phase, LEGAL_TRANSITIONS, transition_to)
    - src/atc_sim/sim/procedure.py (compute_target, advance_leg_if_reached)
    - src/atc_sim/navdata/eggw.py (EGGW_RUNWAY.heading_deg_mag for DEPARTURE_ROLL direction; threshold origin for APPROACH distance)
  </read_first>
  <behavior>
    - TAXI_OUT / TAXI_IN: position frozen; only phase_elapsed_sec accumulates
    - DEPARTURE_ROLL: straight-line acceleration along runway heading (no turning) until rotation speed
    - CLIMB/ENROUTE/DESCENT/APPROACH: full compute_target-driven turn + climb-or-descend + speed change + position move; advance_leg_if_reached called each tick
    - Phase transitions happen only via transition_to when a completion condition is met; phase_elapsed_sec resets to 0.0 on transition
  </behavior>
  <action>
    In `src/atc_sim/sim/aircraft.py`, replace the Phase-1 straight-line `sim_step` with the dispatch shape from RESEARCH Code Examples (lines 466-493). Import `Phase`, `transition_to` from atc_sim.sim.phase and `compute_target`, `advance_leg_if_reached` from atc_sim.sim.procedure and the kinematics helpers from atc_sim.sim.performance. Keep the top-level signature `def sim_step(aircraft: Aircraft, dt: float) -> None`.

    Write a short `sim_step` that: (1) dispatches to one small named helper by phase — `_step_taxi` (TAXI_OUT, TAXI_IN: only `aircraft.phase_elapsed_sec += dt`), `_step_departure_roll` (DEPARTURE_ROLL), `_step_airborne` (CLIMB, ENROUTE, DESCENT, APPROACH), `_step_landed` (LANDED: near-instant pass-through) — keeping each helper independently testable and sim_step itself short (avoid the "scattered inline conditionals" pitfall); (2) after the kinematics helper, if a module-level `_is_phase_complete(aircraft)` returns True, set `aircraft.phase = transition_to(aircraft.phase, _next_phase(aircraft.phase))` and reset `aircraft.phase_elapsed_sec = 0.0`.

    `_next_phase(phase)` returns the single legal successor from `LEGAL_TRANSITIONS[phase]` (the linear chain has exactly one successor per non-terminal phase).

    `_is_phase_complete(aircraft)` (the completion conditions that read aircraft state — kept in aircraft.py, NOT phase.py, to preserve phase.py as a pure leaf): TAXI_OUT/TAXI_IN complete when phase_elapsed_sec >= a module-level taxi duration constant (choose ~15-20 sim seconds, RESEARCH Pattern C `[ASSUMED]`); DEPARTURE_ROLL completes when speed_kt reaches a rotation speed (~140-160kt); CLIMB completes when the aircraft has begun tracking the procedure (e.g. reached a nominal climb altitude / first leg) — since the departure demo is removed from ENROUTE, keep CLIMB->ENROUTE tied to leg progress; APPROACH completes (->LANDED) when distance-to-threshold < ~0.1nm AND altitude_ft < ~50ft. Define these thresholds as named module-level constants with `[ASSUMED]` sourcing comments.

    `_step_departure_roll`: accelerate speed_kt toward the rotation speed via `step_toward_value`, move x_nm/y_nm straight along `EGGW_RUNWAY.heading_deg_mag` using the sin=x/cos=y convention (north-positive y in nm space), no turning.

    `_step_airborne`: call `compute_target(aircraft)`; turn heading via `step_toward_heading` using `turn_rate_deg_per_sec(aircraft.performance.max_bank_deg, aircraft.speed_kt) * dt` as max turn; move altitude via `step_toward_value` selecting climb_rate_fpm when target altitude is above current and descent_rate_fpm when below (converting fpm to per-tick feet via `/60.0 * dt`); change speed via `step_toward_value` bounded by `max_speed_change_kt_per_sec * dt`; move x_nm/y_nm from current heading and speed (convert speed_kt to nm-per-second: kt / 3600.0) using the sin=x/cos=y convention; then call `advance_leg_if_reached(aircraft)`. For APPROACH specifically, target altitude comes from `glidepath_altitude_ft(distance_to_threshold_nm)` (simplified 3-degree glidepath to the runway threshold origin), NOT from compute_target's leg restriction — this is the documented APPROACH scope boundary (no ILS geometry).

    Add `# Phase 3` inline comments at each change point explaining the decision/pitfall it addresses (matching the existing `# Phase 2 (Pitfall A)` comment style).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `sim_step` is a short dispatcher delegating to `_step_taxi`/`_step_departure_roll`/`_step_airborne`/`_step_landed` (no long inline if/elif kinematics block)
    - Phase transitions occur only through `transition_to`; `phase_elapsed_sec` resets to 0.0 on each transition
    - TAXI_OUT/TAXI_IN leave x_nm/y_nm unchanged across a tick (stationary — supports D-05)
    - `_step_airborne` uses `compute_target`, `turn_rate_deg_per_sec`, and per-type climb/descent selection; APPROACH altitude uses `glidepath_altitude_ft`
    - `python -m pytest tests/ -q` passes (test_procedure_following green; boundary green; no regressions). Flow tests remain skipped until demo_traffic exists in 03-05.
  </acceptance_criteria>
  <done>Aircraft executes phase-appropriate kinematics each tick and transitions phases legally; the full suite (minus still-skipped flow tests) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Aircraft state is driven entirely by hardcoded navdata/performance constants; no external/user input this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | Mutable Aircraft state | low | mitigate | `validate_assignment=True` + `Field` bounds (heading in [0,360), altitude/speed >= sensible floors) reject physically-impossible mutations during kinematics — a correctness control against internal logic bugs, not an adversarial boundary. |
| T-03-05 | Information Disclosure | Heading convention drift | low | mitigate | `true_to_magnetic` called exactly once in compute_target keeps `heading_deg` magnetic-consistent with all navdata fields (Pitfall 4), preventing silent true-vs-magnetic mismatch that would corrupt Phase 4/5 comparisons. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` passes (test_procedure_following.py green; test_performance/test_phase_fsm still green; flow tests skipped).
- `python -m pytest tests/test_boundary.py -q` passes (aircraft.py, procedure.py, interpolation.py all headless).
- No circular-import error importing atc_sim.sim.aircraft (procedure.py uses TYPE_CHECKING import).
</verification>

<success_criteria>
- An Aircraft autonomously follows a procedure's legs with restriction look-ahead and legal phase transitions (PROC-01, PERF-02 kinematics).
- Coordinate space unified to nm; no reintroduced pixel-space or true-heading drift.
</success_criteria>

## Artifacts this phase produces (introduced by this plan)

- Evolved `Aircraft` fields: x_nm, y_nm, altitude_ft, speed_kt, heading_deg (magnetic), phase, phase_elapsed_sec, procedure, procedure_leg_index, performance
- `sim_step` dispatcher + helpers `_step_taxi`, `_step_departure_roll`, `_step_airborne`, `_step_landed`, `_is_phase_complete`, `_next_phase`, taxi/rotation/approach threshold constants
- `src/atc_sim/sim/procedure.py`: `Targets` (frozen dataclass), `compute_target`, `_next_altitude_restriction_ft`, `_next_speed_restriction_kt`, `advance_leg_if_reached`
- `AircraftSnapshot` renamed fields x_nm/y_nm (interpolation.py)

<output>
Create `.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-04-SUMMARY.md` when done
</output>
