---
phase: 03-aircraft-performance-flight-phase-fsm-procedure-following
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - src/atc_sim/sim/performance.py
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "Four distinct PerformanceProfile constants exist (737-800, E175, ATR 72-600, CJ2+) with pairwise-differing climb/descent/terminal-speed/bank values"
    - "turn_rate_deg_per_sec returns different rates for different speeds at the same bank angle"
    - "PerformanceProfile is frozen and rejects non-positive rate values"
  artifacts:
    - src/atc_sim/sim/performance.py
  key_links:
    - "FLEET dict maps each profile.name -> PerformanceProfile"
    - "Turn rate is COUPLED to current speed by the shared 1091*tan(bank)/speed formula (Pitfall 7), not an independent per-type constant"
---

## Phase Goal

**As a** controller observing the radar, **I want to** watch aircraft of different types autonomously fly complete departures and arrivals with realistic phase transitions, **so that** the airspace has believable self-driving traffic before Phase 4 instructions exist.

<objective>
Deliver the per-type performance model (D-01, PERF-01): a frozen `PerformanceProfile` Pydantic model, four hand-authored fleet constants spanning a wide performance envelope, and the shared kinematics helper functions that move an aircraft toward a target at its type's limits. This is the leaf module every later slice consumes — it makes `tests/test_performance.py` activate and pass.

Purpose: Behavioral differentiation between aircraft types (success criterion #1) emerges from these numbers plus the speed-coupled turn-rate formula — no new visual symbol system is needed (D-04).
Output: `src/atc_sim/sim/performance.py`.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md
@src/atc_sim/navdata/models.py
@src/atc_sim/navdata/geo.py
@src/atc_sim/sim/aircraft.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PerformanceProfile model + FLEET constants</name>
  <files>src/atc_sim/sim/performance.py</files>
  <read_first>
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md (Pattern A, lines 202-258 — the PerformanceProfile model and the four fleet constant values; "Standard Stack: Aircraft Fleet" for sourcing/adaptation notes)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("performance.py" section — frozen-model shape from navdata/models.py, sourcing-comment convention from navdata/eggw.py)
    - src/atc_sim/navdata/models.py (ILS class lines 16-25: exact frozen ConfigDict + Field(...) bound convention to mirror)
    - tests/test_performance.py (the assertions this module must satisfy)
  </read_first>
  <behavior>
    - FLEET contains exactly 4 profiles; the four differ pairwise on climb_rate_fpm, descent_rate_fpm, terminal_speed_kt, max_bank_deg
    - PerformanceProfile rejects climb_rate_fpm <= 0 (pydantic.ValidationError)
    - PerformanceProfile instances are frozen (assignment raises)
  </behavior>
  <action>
    Create `src/atc_sim/sim/performance.py`. Start with the mandatory sim-core module docstring ending in the exact sentence "This module MUST NOT import pygame — it stays headlessly testable via pytest with no display, enforced by tests/test_boundary.py." (copy the wording verbatim from src/atc_sim/sim/aircraft.py lines 13-15).

    Define `class PerformanceProfile(BaseModel)` with `model_config = ConfigDict(frozen=True)` and these fields with bounds exactly as in RESEARCH Pattern A: `name: str`, `category: str`, `climb_rate_fpm: float = Field(gt=0.0)`, `descent_rate_fpm: float = Field(gt=0.0)`, `terminal_speed_kt: float = Field(gt=0.0)`, `approach_speed_kt: float = Field(gt=0.0)`, `max_bank_deg: float = Field(gt=0.0, lt=45.0)`, `max_speed_change_kt_per_sec: float = Field(gt=0.0)`.

    Instantiate the four fleet constants with the exact adapted in-sim values from RESEARCH Pattern A: `BOEING_737_800` (narrowbody_jet; climb 1800, descent 1500, terminal 250, approach 140, bank 25.0, speed_change 1.5); `EMBRAER_E175` (regional_jet; 2200, 1800, 250, 130, 25.0, 1.8); `ATR_72_600` (turboprop; 1350, 1000, 180, 110, 22.0, 1.2); `CESSNA_CJ2_PLUS` (light_jet; 3500, 2000, 220, 105, 27.0, 2.0). Above each constant, add a sourcing-tag comment in the `[CITED]`/`[ASSUMED]` style used in navdata/eggw.py, noting these are simplified terminal-area in-sim values (NOT the raw real-world Mach-cruise numbers — RESEARCH's "Critical adaptation" note).

    Define `FLEET: dict[str, PerformanceProfile]` as `{p.name: p for p in (BOEING_737_800, EMBRAER_E175, ATR_72_600, CESSNA_CJ2_PLUS)}`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_performance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/atc_sim/sim/performance.py` exists with the "MUST NOT import pygame" docstring sentence
    - `PerformanceProfile` uses `ConfigDict(frozen=True)` and `Field(gt=0.0)` / `Field(gt=0.0, lt=45.0)` bounds exactly as specified
    - `FLEET` has exactly 4 entries keyed by profile name
    - `python -m pytest tests/test_performance.py -x -q` passes (the file activates from skipped to passing)
    - `python -m pytest tests/test_boundary.py -x -q` still passes (no pygame import in the new sim module)
  </acceptance_criteria>
  <done>PerformanceProfile + a 4-type FLEET exist as frozen validated reference data; test_performance.py's model/fleet assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared kinematics helper functions</name>
  <files>src/atc_sim/sim/performance.py</files>
  <read_first>
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md (Pattern A turn_rate_deg_per_sec lines 251-258 and Pitfall 7 coupling rationale; "3° glidepath descent rate" row of the fleet table)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("Module-level pure-function pattern" from navdata/geo.py — single-purpose docstring naming the ONE place this logic lives)
    - src/atc_sim/navdata/geo.py (pure module-level function style with "why reuse this" docstrings)
  </read_first>
  <behavior>
    - turn_rate_deg_per_sec(25, 250) != turn_rate_deg_per_sec(25, 120): turn rate varies with speed at fixed bank
    - Each "toward-target" helper never overshoots its target in a single tick and never exceeds the profile's per-tick rate limit
  </behavior>
  <action>
    In `src/atc_sim/sim/performance.py` add these pure module-level functions (no methods on the model). Use `import math` at module top.

    `turn_rate_deg_per_sec(bank_deg, speed_kt) -> float` returning `(1091.0 * math.tan(math.radians(bank_deg))) / speed_kt` with a docstring citing SKYbrary and naming this as the ONE turn-rate formula (RESEARCH Pattern A). This function must be called with the aircraft's CURRENT speed so per-type turn differences emerge from differing terminal speeds without a separate turn constant.

    `step_toward_heading(current_deg, target_deg, max_turn_deg) -> float` returning the new heading after turning at most `max_turn_deg` toward `target_deg` along the shortest angular path across the 0/360 wrap (mirror the wrap logic already used in src/atc_sim/sim/interpolation.py `_lerp_angle_deg`), clamped so it never overshoots. Keep the sin=x/cos=y, 0deg=north, clockwise compass convention documented in geo.py — do not reinvent a different sign convention.

    `step_toward_value(current, target, max_delta) -> float` — a single generic helper that moves `current` toward `target` by at most `max_delta`, never overshooting; reuse it for both altitude (max_delta = rate_fpm/60 * dt) and speed (max_delta = max_speed_change_kt_per_sec * dt). Document that climb vs descent rate selection (climb_rate_fpm when target above current, descent_rate_fpm when below) is the CALLER's responsibility (done in aircraft.py's dispatch in plan 03-04).

    `glidepath_altitude_ft(distance_to_threshold_nm) -> float` returning `distance_to_threshold_nm * 300.0` clamped to `>= 0.0` (the simplified 3-degree "rule of three" from RESEARCH Pattern C, used only by APPROACH in a later plan — NOT real localizer/glideslope geometry).
  </action>
  <verify>
    <automated>python -m pytest tests/test_performance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `turn_rate_deg_per_sec` implements `1091*tan(radians(bank))/speed` and returns different values for (25,250) vs (25,120)
    - `step_toward_value` never overshoots: `step_toward_value(0, 10, 3)` == 3 and `step_toward_value(0, 2, 3)` == 2
    - `step_toward_heading` turns the short way across the 0/360 seam and never overshoots the target
    - `glidepath_altitude_ft(10)` == 3000.0 and `glidepath_altitude_ft(0)` == 0.0
    - `python -m pytest tests/test_performance.py -x -q` passes
  </acceptance_criteria>
  <done>Shared, speed-coupled kinematics helpers exist as pure functions; test_performance.py fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Performance profiles are hardcoded constants; no external/user input this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | PerformanceProfile internal state | low | mitigate | `ConfigDict(frozen=True)` + `Field(gt=0.0)` / `Field(lt=45.0)` bounds reject physically-impossible profile values (negative climb rate, >=45deg bank) at construction — a correctness control against internal logic bugs, not an adversarial boundary (none exists). |
</threat_model>

<verification>
- `python -m pytest tests/test_performance.py -x -q` passes.
- `python -m pytest tests/test_boundary.py -x -q` passes (performance.py stays headless).
- `python -m pytest tests/ -q` shows no regressions.
</verification>

<success_criteria>
- Four visibly-differentiated performance profiles exist (PERF-01, D-01) with turn rate coupled to speed (Pitfall 7).
- No new visual symbol system introduced (D-04 respected — differentiation is behavioral/numeric only).
</success_criteria>

## Artifacts this phase produces (introduced by this plan)

- `src/atc_sim/sim/performance.py`
- `PerformanceProfile` (frozen Pydantic model)
- Constants `BOEING_737_800`, `EMBRAER_E175`, `ATR_72_600`, `CESSNA_CJ2_PLUS`, `FLEET`
- Functions `turn_rate_deg_per_sec`, `step_toward_heading`, `step_toward_value`, `glidepath_altitude_ft`

<output>
Create `.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-02-SUMMARY.md` when done
</output>
