---
phase: 02-navdata-coordinate-projection
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/atc_sim/sim/aircraft.py
  - src/atc_sim/sim/interpolation.py
  - tests/test_interpolation.py
autonomous: true
requirements: [RADAR-04]
must_haves:
  truths:
    - "interpolate() lerps smoothly across any position jump — no wrap-at-edge snap-to-curr special case remains"
    - "sim_step no longer teleport-wraps the aircraft at the canvas edge; the test aircraft flies a straight, non-wrapping path"
    - "the test aircraft spawns at the radar center (the runway-threshold projection origin) so it starts within the real EGGW geography rather than in a corner"
  artifacts:
    - "src/atc_sim/sim/interpolation.py with the Phase-1 wrap-skip removed and the now-unused CANVAS import dropped"
    - "src/atc_sim/sim/aircraft.py with the canvas-edge wrap removed and spawn repositioned"
    - "updated tests/test_interpolation.py (wrap tests replaced by non-wrap assertions)"
  key_links:
    - "removing the interpolation wrap-skip and the sim_step wrap together prevents both the teleport bug and the streak bug once Phase 2's real-world-scale coordinates are in play (Pitfall A)"
---

<objective>
Retire the Phase-1-only wrap-at-edge behavior that the codebase's own comments flag for removal "once Phase 2 replaces wrap-at-edge with real navdata paths" (Pitfall A). Remove `interpolate()`'s snap-to-curr special case AND `sim_step`'s canvas-edge teleport-wrap together (removing only one would regress: the wrap-skip exists precisely to hide the wrap's teleport streak). Reposition the placeholder test aircraft to the radar center — which is the runway-threshold projection origin — so it starts inside the real EGGW geography.

Purpose: Ensure real-world-scale positions interpolate correctly (no teleport, no streak) as required for RADAR-04's projection to render smooth motion once real coordinates are on the canvas.
Output: cleaned `sim/interpolation.py` and `sim/aircraft.py`, updated `tests/test_interpolation.py`.

Scope guard: this plan does NOT add procedure-following, performance modeling, or lat/lon aircraft state (all Phase 3). The aircraft keeps its simple Phase-1 straight-line, fixed-heading, pixel-space motion — only the wrap is removed and the spawn point moved.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md
@.planning/phases/02-navdata-coordinate-projection/02-PATTERNS.md
@src/atc_sim/sim/aircraft.py
@src/atc_sim/sim/interpolation.py
@tests/test_interpolation.py
</context>

## Phase Goal

**As a** controller, **I want to** see EGGW's real runway 25, one SID, and one STAR laid out on the radar at their true geographic positions, **so that** I can work traffic against the same real-world geometry the sim will later use for separation.

> This plan is the sim-core cleanup that lets real-world-scale motion render correctly (no teleport/streak) on the real-geography radar.

<tasks>

<task type="auto">
  <name>Task 1: Remove sim_step canvas-edge wrap and reposition the test aircraft spawn (sim/aircraft.py)</name>
  <files>src/atc_sim/sim/aircraft.py, tests/test_interpolation.py</files>
  <read_first>
    - src/atc_sim/sim/aircraft.py (current sim_step wrap block lines 45-53, spawn_default line 29-32, CANVAS_WIDTH/CANVAS_HEIGHT constants lines 17-18)
    - .planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md (Pitfall A — the wrap behavior is Phase-1-only and in-scope to remove this phase)
    - .planning/phases/02-navdata-coordinate-projection/02-PATTERNS.md (interpolation section — notes CANVAS_WIDTH/HEIGHT may become obsolete; check usage before removing constants)
    - tests/test_interpolation.py (test_sim_step_wraps_at_edge lines 91-95 and test_sim_step_moves_along_heading lines 83-88 — the first must be replaced, the second stays valid)
  </read_first>
  <action>In sim/aircraft.py, delete the entire canvas-edge wrap block in `sim_step` (the four `if aircraft.x/y > / <` branches that add/subtract CANVAS_WIDTH/CANVAS_HEIGHT) so `sim_step` becomes pure straight-line motion along heading (the `math.sin`/`-math.cos` position update stays exactly as-is — the compass convention is unchanged). Update `spawn_default` to spawn at the radar center: `x=CANVAS_WIDTH / 2, y=CANVAS_HEIGHT / 2` (= pixel (640, 400) = the runway-threshold projection origin per navdata/geo.py ORIGIN; add an inline comment saying so and that this is still Phase-1-level placeholder motion, no procedure following). Keep the CANVAS_WIDTH/CANVAS_HEIGHT module constants (they still define the fixed canvas and are now used only by spawn_default). Replace the D-02 wrap comments with a note that wrap-at-edge was removed in Phase 2 (Pitfall A). In tests/test_interpolation.py, replace `test_sim_step_wraps_at_edge` with `test_sim_step_does_not_wrap_at_edge`: an aircraft near the right edge advanced by sim_step keeps increasing x past CANVAS_WIDTH (no wrap-around to a small x). Leave test_sim_step_moves_along_heading unchanged.</action>
  <verify>
    <automated>pytest tests/test_interpolation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CANVAS_WIDTH' src/atc_sim/sim/aircraft.py` shows the constant still defined and used only in spawn_default (no occurrences inside sim_step's body).
    - `grep -n 'def sim_step' -A6 src/atc_sim/sim/aircraft.py` shows no `if aircraft.x >` / `aircraft.x -=` wrap branches remain.
    - `spawn_default` returns an Aircraft at x == CANVAS_WIDTH/2 and y == CANVAS_HEIGHT/2.
    - New test_sim_step_does_not_wrap_at_edge passes (x increases past the canvas width instead of wrapping); test_sim_step_moves_along_heading still passes.
  </acceptance_criteria>
  <done>sim_step is pure non-wrapping straight-line motion and the placeholder aircraft spawns at the radar center / projection origin.</done>
</task>

<task type="auto">
  <name>Task 2: Remove interpolate() wrap-skip special case and drop the now-unused CANVAS import (sim/interpolation.py)</name>
  <files>src/atc_sim/sim/interpolation.py, tests/test_interpolation.py</files>
  <read_first>
    - src/atc_sim/sim/interpolation.py (interpolate() lines 42-56 — the wrap-skip block lines 43-50 to remove; the import line 18 `from atc_sim.sim.aircraft import CANVAS_HEIGHT, CANVAS_WIDTH, Aircraft`; the _lerp/_lerp_angle_deg helpers lines 32-39 which stay)
    - .planning/phases/02-navdata-coordinate-projection/02-PATTERNS.md (interpolation section — the wrap-skip comment itself says "removed once Phase 2 replaces wrap-at-edge"; keep _lerp/_lerp_angle_deg unchanged; drop unused CANVAS import)
    - tests/test_interpolation.py (test_wrap_skip_snaps_to_current lines 68-80 — must be replaced; CANVAS_WIDTH/CANVAS_HEIGHT import line 11)
  </read_first>
  <action>In sim/interpolation.py, delete the wrap-skip special case in `interpolate()` (the `if abs(curr.x - prev.x) > CANVAS_WIDTH / 2 or ...: return curr` block and its comment) so `interpolate()` always returns the plain `_lerp`/`_lerp_angle_deg` snapshot. Change the import at line 18 to `from atc_sim.sim.aircraft import Aircraft` (drop the now-unused CANVAS_WIDTH, CANVAS_HEIGHT). Leave `_lerp`, `_lerp_angle_deg`, `capture_state`, and `AircraftSnapshot` unchanged. In tests/test_interpolation.py, replace `test_wrap_skip_snaps_to_current` with `test_large_jump_now_lerps` asserting that a large prev→curr position jump (e.g. prev.x=1270, curr.x=5) now interpolates to the midpoint (~637.5 at alpha=0.5) rather than snapping to curr; update the file's import to drop CANVAS_WIDTH/CANVAS_HEIGHT if no remaining test uses them (adjust any remaining references such as the old wrap test's fixtures accordingly).</action>
  <verify>
    <automated>pytest tests/test_interpolation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CANVAS_WIDTH' src/atc_sim/sim/interpolation.py` returns 0 (import and special case both gone).
    - `grep -n 'return curr' src/atc_sim/sim/interpolation.py` returns nothing inside interpolate() (no snap-to-curr path).
    - New test_large_jump_now_lerps passes (large jump lerps to midpoint, does not snap to curr).
    - test_interpolate_does_not_mutate_inputs, test_position_lerp_midpoint, and test_heading_interpolation_shortest_path all still pass.
  </acceptance_criteria>
  <done>interpolate() is a plain lerp with no Phase-1 wrap special case; real-world-scale jumps interpolate smoothly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sim tick → render interpolation | interpolate() reads two frozen snapshots and returns a throwaway third; it must never mutate the live Aircraft (CORE-03 still holds) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | interpolate() mutating live sim state during cleanup | low | mitigate | test_interpolate_does_not_mutate_inputs (retained) asserts inputs are unchanged and the result is a distinct object |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan |
</threat_model>

<verification>
- `pytest tests/test_interpolation.py -x` green with the replaced tests.
- `pytest` full suite green (no regression to clock/render smoke tests).
</verification>

<success_criteria>
The Phase-1 wrap-at-edge behavior is fully removed from both sim_step and interpolate(), real-world-scale position jumps interpolate smoothly (no teleport, no streak), and the placeholder aircraft spawns at the radar center / projection origin — all proven by updated headless tests.
</success_criteria>

## Artifacts this phase produces (Plan 04)

Modified files (behavior changes, no new public symbols):
- `src/atc_sim/sim/aircraft.py` — `sim_step` loses its wrap block; `spawn_default` repositioned to (CANVAS_WIDTH/2, CANVAS_HEIGHT/2)
- `src/atc_sim/sim/interpolation.py` — `interpolate` loses the wrap-skip special case; CANVAS import dropped
- `tests/test_interpolation.py` — new symbols: `test_sim_step_does_not_wrap_at_edge`, `test_large_jump_now_lerps` (replacing `test_sim_step_wraps_at_edge`, `test_wrap_skip_snaps_to_current`)

<output>
Create `.planning/phases/02-navdata-coordinate-projection/02-04-SUMMARY.md` when done.
</output>
