---
phase: 03-aircraft-performance-flight-phase-fsm-procedure-following
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_performance.py
  - tests/test_phase_fsm.py
  - tests/test_procedure_following.py
  - tests/test_departure_flow.py
  - tests/test_arrival_flow.py
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PROC-01]

must_haves:
  truths:
    - "Five test files exist, one per phase requirement (PERF-01/02/03/04, PROC-01)"
    - "conftest.py exposes a headless 'step N sim ticks and record the phase sequence observed' helper reused by both flow tests"
    - "Every test guards its import of not-yet-existing sim modules so the full suite stays green (skipped) until implementation lands"
  artifacts:
    - tests/conftest.py
    - tests/test_performance.py
    - tests/test_phase_fsm.py
    - tests/test_procedure_following.py
    - tests/test_departure_flow.py
    - tests/test_arrival_flow.py
  key_links:
    - "Each test file maps 1:1 to a requirement per 03-VALIDATION.md Req->Test map"
    - "Flow tests import the shared conftest phase-recorder rather than duplicating tick-stepping logic"
---

## Phase Goal

**As a** controller observing the radar, **I want to** watch aircraft of different types autonomously fly complete departures (off the OLNEY 2B SID) and arrivals (down the DET 2A STAR) with realistic, never-skipped flight-phase transitions, **so that** the airspace has believable self-driving traffic before I gain the ability to issue instructions in Phase 4.

*(Derived faithfully from the ROADMAP Phase 3 outcome goal; this project runs every phase in MVP mode with outcome-shaped ROADMAP goals, matching completed Phases 1-2.)*

<objective>
Create the Nyquist test scaffold for Phase 3 FIRST, before any implementation. This is the MVP "failing end-to-end test" step: the five requirement tests and the shared headless fixture are authored now so every subsequent implementation plan has an automated green/red signal wired from the moment its module lands.

Purpose: Guarantee no requirement is implemented without an executable check, and give the demo-loop flow (departure/arrival) a headless test harness that does not require pygame or a display.
Output: `tests/conftest.py` + five test files. Because the modules under test do not exist yet, each test guards its imports with `pytest.importorskip(...)` so the existing suite stays green (tests report as skipped) until the corresponding implementation plan activates them.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-VALIDATION.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md
@tests/test_clock.py
@tests/test_boundary.py
@src/atc_sim/sim/clock.py
@src/atc_sim/navdata/eggw.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared headless phase-recorder fixture in tests/conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/test_clock.py (existing headless pure-function test style; SimClock usage pattern)
    - src/atc_sim/sim/clock.py (SimClock.advance(real_dt, on_tick) signature, TICK_DT=0.5)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-VALIDATION.md ("Wave 0 Requirements" — the conftest fixture bullet)
  </read_first>
  <action>
    Create `tests/conftest.py` (it does not exist yet). Add one pytest fixture named `phase_recorder` that returns a callable helper `record_phases(aircraft, step_fn, n_ticks, tick_dt=0.5)`. The helper calls `step_fn(aircraft, tick_dt)` exactly `n_ticks` times and, after each call, appends the current value of `aircraft.phase` to an ordered list; it returns that list with consecutive duplicates collapsed (so an aircraft that stays in CLIMB for 20 ticks contributes a single CLIMB entry), giving the observed phase-transition SEQUENCE. Keep the helper generic: it must NOT import any `atc_sim.sim.*` module itself — `aircraft` and `step_fn` are supplied by the caller, so this file has zero import dependency on code that does not exist yet and never triggers a collection error. Add a module docstring stating this fixture exists so the two flow tests (test_departure_flow, test_arrival_flow) do not each re-implement tick stepping. Do NOT import pygame anywhere.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` exists and defines a `phase_recorder` fixture returning a `record_phases`-style callable
    - The helper collapses consecutive duplicate phases and returns an ordered list
    - `tests/conftest.py` contains no `import atc_sim.sim` and no `import pygame`
    - `python -m pytest tests/ -q` still passes (existing Phase 1/2 tests remain green; no collection errors introduced)
  </acceptance_criteria>
  <done>conftest.py provides a reusable, dependency-free headless phase-sequence recorder and the suite still collects and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Five requirement test files (import-guarded stubs with real assertions)</name>
  <files>tests/test_performance.py, tests/test_phase_fsm.py, tests/test_procedure_following.py, tests/test_departure_flow.py, tests/test_arrival_flow.py</files>
  <read_first>
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-VALIDATION.md (Req->Test map; exact file names and per-requirement behaviors)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md (Pattern A/B/D and the "Restriction look-ahead omitted" pitfall for the exact expected numeric behaviors)
    - tests/test_navdata.py (frozen-model + real-navdata-correctness assertion style) if present; otherwise tests/test_clock.py
    - src/atc_sim/navdata/eggw.py (OLNEY_2B_SID / DET_2A_STAR leg data the tests assert against)
  </read_first>
  <action>
    Create five test files. Each begins with a top-level `import pytest` and guards the module(s) it exercises via `pytest.importorskip("atc_sim.sim.<module>")` at module scope so that, until that module exists, the whole file is skipped (keeping `pytest tests/ -q` green) and, once it exists, the real assertions run.

    - `tests/test_performance.py` (PERF-01): importorskip `atc_sim.sim.performance`. Assert `FLEET` contains exactly 4 profiles; assert the four profiles have pairwise-distinct `climb_rate_fpm`, `descent_rate_fpm`, `terminal_speed_kt`, and `max_bank_deg` sets (i.e. not all equal); assert `PerformanceProfile` rejects a non-positive `climb_rate_fpm` (expect `pydantic.ValidationError`) and is frozen (assigning to a field raises); assert `turn_rate_deg_per_sec(bank_deg=25, speed_kt=250)` differs from `turn_rate_deg_per_sec(bank_deg=25, speed_kt=120)` (turn rate coupled to speed, per RESEARCH Pattern A).
    - `tests/test_phase_fsm.py` (PERF-02): importorskip `atc_sim.sim.phase`. Assert `Phase` has exactly 8 members named TAXI_OUT, DEPARTURE_ROLL, CLIMB, ENROUTE, DESCENT, APPROACH, LANDED, TAXI_IN; assert `LEGAL_TRANSITIONS` has a key for every `Phase` member; assert `transition_to` walks the full linear chain TAXI_OUT->DEPARTURE_ROLL->CLIMB->ENROUTE->DESCENT->APPROACH->LANDED->TAXI_IN without error; assert `transition_to(Phase.TAXI_OUT, Phase.CLIMB)` raises `ValueError` (skip is illegal); assert no member named LOC_CAPTURED / GS_CAPTURED / REMOVED exists.
    - `tests/test_procedure_following.py` (PROC-01): importorskip `atc_sim.sim.procedure` and `atc_sim.sim.aircraft`. Build an arrival `Aircraft` positioned near the DET fix on `DET_2A_STAR` with `procedure_leg_index` set to the LOFFO leg (index 1, which carries NO altitude restriction). Assert `compute_target(aircraft).altitude_ft == 8000` (look-ahead to ABBOT's FL080, NOT held at FL170) — this is the exact regression the "Restriction look-ahead omitted" pitfall requires. Assert `compute_target(aircraft).heading_deg` equals the magnetic bearing (verify it was passed through `true_to_magnetic` by comparing against `true_to_magnetic(<computed true bearing>)` within a small tolerance). Assert `advance_leg_if_reached` increments `procedure_leg_index` when the aircraft is placed within the capture radius of its current leg's fix.
    - `tests/test_departure_flow.py` (PERF-03): importorskip `atc_sim.sim.demo_traffic`. Use the `phase_recorder` fixture. Spawn a departure via `spawn_departure(...)`, step it with the demo step function for enough ticks to complete the flight, and assert the recorded phase sequence is exactly [TAXI_OUT, DEPARTURE_ROLL, CLIMB, ENROUTE] with no skips or repeats-out-of-order; assert the aircraft is removed from an active-traffic list once `procedure_leg_index` exhausts `OLNEY_2B_SID.legs` while in ENROUTE.
    - `tests/test_arrival_flow.py` (PERF-04): importorskip `atc_sim.sim.demo_traffic`. Use `phase_recorder`. Spawn an arrival via `spawn_arrival(...)`; assert it starts in DESCENT at `altitude_ft == 17000` (DET fix FL170); assert the recorded sequence is exactly [DESCENT, APPROACH, LANDED, TAXI_IN]; assert it is removed from active traffic after the TAXI_IN timer elapses.

    Do NOT import pygame in any test. Where a test needs geometric expected values (bearings), compute them in-test from `navdata.geo` helpers rather than hardcoding magic numbers.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist with the exact names above
    - Each file's module-scope `pytest.importorskip(...)` names the module it exercises, so the file is skipped (not errored) while that module is absent
    - `python -m pytest tests/ -q` passes with the five new files reported as skipped and all pre-existing tests still passing (zero collection errors, zero failures)
    - test_procedure_following.py asserts `compute_target(...).altitude_ft == 8000` on the LOFFO leg (look-ahead), not 17000
    - test_departure_flow.py / test_arrival_flow.py consume the `phase_recorder` fixture rather than re-implementing tick stepping
  </acceptance_criteria>
  <done>Five requirement tests exist as import-guarded specs; suite is green (new tests skipped) and each will activate automatically when its module is implemented in a later wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds no network, file, or user-input boundary. All navdata/performance/procedure data are hardcoded Python constants; the player cannot issue instructions until Phase 4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Test scaffold | low | accept | Tests are developer-authored, run locally; no untrusted input. No new packages installed (RESEARCH Package Legitimacy Audit: nothing to audit). |
</threat_model>

<verification>
- `python -m pytest tests/ -q` passes with 5 new files skipped (import-guarded) and all Phase 1/2 tests green.
- `tests/conftest.py` and all five test files contain no `import pygame` (consistent with tests/test_boundary.py intent for sim-core testability).
</verification>

<success_criteria>
- Nyquist scaffold complete: one automated test per Phase 3 requirement plus the shared headless recorder.
- Suite remains runnable and green; no collection errors from referencing modules that do not exist yet.
</success_criteria>

## Artifacts this phase produces (introduced by this plan)

- `tests/conftest.py` — `phase_recorder` fixture / `record_phases(aircraft, step_fn, n_ticks, tick_dt)` helper
- `tests/test_performance.py`, `tests/test_phase_fsm.py`, `tests/test_procedure_following.py`, `tests/test_departure_flow.py`, `tests/test_arrival_flow.py`

<output>
Create `.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-01-SUMMARY.md` when done
</output>
