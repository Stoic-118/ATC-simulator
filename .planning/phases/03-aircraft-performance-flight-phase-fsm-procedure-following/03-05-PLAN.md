---
phase: 03-aircraft-performance-flight-phase-fsm-procedure-following
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-04]
files_modified:
  - src/atc_sim/sim/demo_traffic.py
autonomous: true
requirements: [PERF-03, PERF-04]

must_haves:
  truths:
    - "spawn_departure creates a TAXI_OUT aircraft at a stand position on the OLNEY 2B SID"
    - "spawn_arrival creates a DESCENT aircraft at the DET entry fix at FL170 with a realistic initial speed"
    - "A departure is removed from active traffic once procedure_leg_index exhausts OLNEY_2B_SID.legs while in ENROUTE"
    - "An arrival is removed from active traffic after its TAXI_IN timer elapses"
    - "When both aircraft are gone, a fresh departure/arrival pair spawns automatically (demo loops)"
    - "The spawned aircraft type rotates across loop iterations so >=3 distinct types are observed within a couple of loops"
  artifacts:
    - src/atc_sim/sim/demo_traffic.py
  key_links:
    - "update_demo_traffic is called once per sim tick from app.py (03-06); it steps every aircraft and performs removal/respawn"
    - "Removal is list membership, not a Phase value (RESEARCH 'Removal is not a phase')"
    - "Type rotation is a round-robin over FLEET, advanced per loop-restart per slot"
---

## Phase Goal

**As a** controller observing the radar, **I want to** watch a full departure and a full arrival play out end-to-end and then automatically repeat with different aircraft types, **so that** I can leave the sim running and continuously verify the autonomous lifecycle.

<objective>
Deliver the demo harness (D-02, D-03, PERF-03, PERF-04): module-level spawn functions for a departure and an arrival, a per-tick orchestration entry point that steps every aircraft and applies removal/respawn conditions, and fleet type-rotation so success criterion #1 ("at least 3 distinct aircraft types visibly differentiated during flight") is actually exercised by the running app. This makes both flow tests (`tests/test_departure_flow.py`, `tests/test_arrival_flow.py`) activate and pass.

Purpose: The two-hardcoded-aircraft demo stands in for the Phase 6 scenario loader (D-02); isolating spawn/removal/loop logic in one module means Phase 6 can replace only this module.
Output: `src/atc_sim/sim/demo_traffic.py`.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md
@.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md
@src/atc_sim/sim/aircraft.py
@src/atc_sim/sim/phase.py
@src/atc_sim/sim/performance.py
@src/atc_sim/navdata/eggw.py
@src/atc_sim/navdata/geo.py
@tests/test_departure_flow.py
@tests/test_arrival_flow.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spawn_departure / spawn_arrival + per-tick update with removal & looping</name>
  <files>src/atc_sim/sim/demo_traffic.py</files>
  <read_first>
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-RESEARCH.md (Architectural Responsibility Map "Demo-harness" row; Pattern B spawn-phase note; "Removal-from-active-traffic treated as a phase-FSM transition" pitfall; Open Question 2 stand coordinates)
    - .planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-PATTERNS.md ("demo_traffic.py" section — spawn_default style, app.py on_tick orchestration style, type-rotation)
    - src/atc_sim/sim/aircraft.py (Aircraft constructor fields; sim_step signature)
    - src/atc_sim/navdata/eggw.py (OLNEY_2B_SID, DET_2A_STAR, EGGW_RUNWAY; DET fix FL170 restriction is the arrival spawn altitude)
    - src/atc_sim/navdata/geo.py (project_to_local_xy_nm to place the arrival at the DET fix in nm space)
    - src/atc_sim/sim/performance.py (FLEET for type rotation)
  </read_first>
  <behavior>
    - spawn_departure returns an Aircraft with phase=TAXI_OUT, procedure=OLNEY_2B_SID, procedure_leg_index=0, positioned at a stand near the threshold origin
    - spawn_arrival returns an Aircraft with phase=DESCENT, procedure=DET_2A_STAR, altitude_ft=17000, positioned at the DET fix in nm, at a realistic initial terminal speed
    - A departure in ENROUTE with procedure_leg_index >= len(OLNEY_2B_SID.legs) is removed from the list
    - An arrival whose TAXI_IN phase_elapsed_sec exceeds the taxi duration is removed
    - When the list becomes empty, a fresh departure+arrival pair is spawned with the next rotated types
  </behavior>
  <action>
    Create `src/atc_sim/sim/demo_traffic.py` with the mandatory "MUST NOT import pygame" docstring sentence. Import `Aircraft`, `sim_step` from atc_sim.sim.aircraft; `Phase` from atc_sim.sim.phase; `FLEET` (and the four profile constants) from atc_sim.sim.performance; `OLNEY_2B_SID`, `DET_2A_STAR` from atc_sim.navdata.eggw; `project_to_local_xy_nm` from atc_sim.navdata.geo.

    Define a module-level `_FLEET_ROTATION` list fixing a deterministic type order (e.g. [BOEING_737_800, EMBRAER_E175, ATR_72_600, CESSNA_CJ2_PLUS]) and module-level round-robin index state for the departure slot and the arrival slot independently (so within 1-2 loop iterations at least 3 — in practice all 4 — types have flown; RESEARCH "3 distinct types visibly differentiated" pitfall). Encapsulate the rotation state so tests can reset it (e.g. a small state object or a reset helper).

    `spawn_departure(performance) -> Aircraft`: construct an Aircraft with phase=Phase.TAXI_OUT, procedure=OLNEY_2B_SID, procedure_leg_index=0, performance=the passed profile, a stand position expressed directly in local nm a short distance off the threshold origin (Discretion/Open Question 2 — e.g. x_nm ~ 0.2, y_nm ~ -0.2; a one-line constant, document as `[ASSUMED]`), altitude_ft=0.0, a low initial speed_kt (>0 to satisfy the Field bound; e.g. taxi speed), heading_deg set to EGGW_RUNWAY.heading_deg_mag. Add a comment explaining the position choice (RESEARCH Open Question 2).

    `spawn_arrival(performance) -> Aircraft`: construct an Aircraft with phase=Phase.DESCENT, procedure=DET_2A_STAR, procedure_leg_index=0, performance=the passed profile, position = project_to_local_xy_nm(DET fix lat/lon) so it starts exactly at the DET entry fix in nm, altitude_ft=17000.0 (DET fix FL170 restriction — PERF-04 "realistic initial altitude"), a realistic initial terminal speed (e.g. performance.terminal_speed_kt), heading_deg pointing toward the next leg (compute via the same true->magnetic path, or leave the airborne kinematics to converge on tick 1). Add a comment citing the FL170 sourcing.

    `update_demo_traffic(aircraft_list, dt) -> None`: (1) call `sim_step(ac, dt)` for every aircraft in the list; (2) remove any DEPARTURE whose `phase == Phase.ENROUTE and procedure_leg_index >= len(ac.procedure.legs)` (RESEARCH removal condition — list membership, NOT a REMOVED phase); (3) remove any ARRIVAL whose `phase == Phase.TAXI_IN and phase_elapsed_sec >= <taxi duration>`; (4) if the list is now empty, advance the rotation indices and spawn a fresh `spawn_departure(next_departure_type)` + `spawn_arrival(next_arrival_type)` pair into the list (D-03 looping). Mutate the passed list in place (app.py owns it).

    Do NOT add a REMOVED/GONE Phase member; removal stays a list operation (RESEARCH pitfall). Do NOT add pygame or any render logic here.
  </action>
  <verify>
    <automated>python -m pytest tests/test_departure_flow.py tests/test_arrival_flow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `spawn_departure` returns phase TAXI_OUT on OLNEY_2B_SID; `spawn_arrival` returns phase DESCENT on DET_2A_STAR at altitude_ft == 17000.0
    - Departure phase sequence observed via the flow test is exactly [TAXI_OUT, DEPARTURE_ROLL, CLIMB, ENROUTE] and it is removed once OLNEY_2B_SID legs exhaust in ENROUTE
    - Arrival phase sequence observed is exactly [DESCENT, APPROACH, LANDED, TAXI_IN] and it is removed after the TAXI_IN timer
    - `update_demo_traffic` respawns a fresh pair when the list empties, with rotated types (a test stepping through >=2 full loops observes >=3 distinct performance profiles across departures)
    - No Phase member named REMOVED/GONE was added
    - `python -m pytest tests/test_departure_flow.py tests/test_arrival_flow.py -x -q` passes; `python -m pytest tests/test_boundary.py -q` passes
  </acceptance_criteria>
  <done>The demo harness spawns, steps, removes, and re-spawns rotated departure/arrival pairs headlessly; both flow tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Demo traffic is generated from hardcoded procedures/profiles; no scenario file or user input this phase (scenario loading is Phase 6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Denial of Service | Demo loop unbounded growth | low | mitigate | `update_demo_traffic` removes each aircraft on its terminal condition before respawning, and respawns only when the list is empty, so active-traffic size stays bounded (never leaks aircraft) across indefinite looping. |
</threat_model>

<verification>
- `python -m pytest tests/test_departure_flow.py tests/test_arrival_flow.py -x -q` passes.
- `python -m pytest tests/ -q` all green (every Phase 3 requirement test now active and passing).
- `python -m pytest tests/test_boundary.py -q` passes (demo_traffic.py headless).
</verification>

<success_criteria>
- A full departure and a full arrival run end-to-end headlessly, are removed on completion, and the demo loops with rotated types (PERF-03, PERF-04, D-02, D-03).
- Removal modeled as list membership, not a phase (RESEARCH pitfall respected).
</success_criteria>

## Artifacts this phase produces (introduced by this plan)

- `src/atc_sim/sim/demo_traffic.py`
- Functions `spawn_departure`, `spawn_arrival`, `update_demo_traffic`
- Module-level `_FLEET_ROTATION` + rotation-state (with a test-resettable helper), stand-position and taxi-duration constants

<output>
Create `.planning/phases/03-aircraft-performance-flight-phase-fsm-procedure-following/03-05-SUMMARY.md` when done
</output>
