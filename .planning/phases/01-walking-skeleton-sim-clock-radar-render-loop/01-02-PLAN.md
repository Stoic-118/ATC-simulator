---
phase: 01-walking-skeleton-sim-clock-radar-render-loop
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/atc_sim/sim/clock.py
  - tests/test_clock.py
autonomous: true
requirements: [CORE-01, CORE-02]
user_setup: []

must_haves:
  truths:
    - "Tick count over a fixed simulated wall-time is identical regardless of how many advance() calls split that time — render-frame-rate independence (CORE-01)"
    - "A single 3.0s real_dt drains at most MAX_TICKS_PER_FRAME (5) ticks in one advance() call, returning without exception or unbounded loop (CORE-02)"
    - "After a capped stall the accumulator is reset (backlog dropped, tracked in dropped_tick_seconds), not retained to burst on a later frame (CORE-02, spiral-of-death prevention)"
    - "advance() returns alpha in [0.0, 1.0) representing fractional progress toward the next tick"
    - "on_tick is invoked exactly once per drained tick with tick_dt as its argument"
  artifacts:
    - src/atc_sim/sim/clock.py
    - tests/test_clock.py
  key_links:
    - "SimClock.advance drains ticks by invoking the on_tick callback — clock.py imports no aircraft type and no pygame, staying headlessly testable"
  prohibitions:
    - statement: "sim/clock.py does not import pygame"
      status: enforced
      verification: "tests/test_boundary.py (from plan 01-01) covers this file automatically once created"
---

<objective>
Implement the fixed-timestep accumulator sim clock — the single riskiest piece of code in the project — test-first. This one mechanism satisfies CORE-01 (frame-rate-independent motion) and CORE-02 (capped catch-up / spiral-of-death prevention) simultaneously and is the seam every later phase's timing depends on.

Purpose: Prove, with fast deterministic headless tests, that render frame rate cannot affect simulated time and that a stall degrades gracefully into bounded slow-motion rather than freezing or leaping.
Output: `sim/clock.py` (`SimClock`) plus a green `tests/test_clock.py`.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for the accumulator clock</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 178-233 Pattern 1; lines 556-589 Validation Architecture / Phase Requirements→Test Map)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md (lines 33-69 clock.py source + lines 180-191 required test cases)
  </read_first>
  <files>tests/test_clock.py</files>
  <behavior>
    - test_tick_count_independent_of_call_frequency: feeding a total of e.g. 5.0 simulated seconds as ONE advance(5.0) call vs. many small advance(dt) calls that sum to the same wall time yields the SAME cumulative tick count and sim_time — but note the single 5.0s call is subject to the MAX_FRAME_TIME clamp and the per-frame cap, so construct the test as: N frames of a fixed small dt (e.g. 1/60) accumulated over the same simulated wall time produce a tick count matching floor(total_time / TICK_DT), independent of the fps chosen (compare 30fps vs 120fps splits of the same 5s → identical tick totals).
    - test_stall_caps_ticks_per_frame: a single advance(3.0) drains at most MAX_TICKS_PER_FRAME (5) ticks, invokes on_tick at most 5 times, returns a float, and does not hang.
    - test_backlog_beyond_cap_is_dropped: after the capped advance(3.0), the internal accumulator is reset toward 0 and dropped_tick_seconds is > 0, so a subsequent advance(0.0) drains ZERO further ticks (no retained backlog burst).
    - test_advance_returns_alpha_in_range: advance() return value is always in [0.0, 1.0).
    - test_on_tick_receives_tick_dt: on_tick is called with tick_dt (0.5 at 2Hz) as its argument.
  </behavior>
  <action>
    Write `tests/test_clock.py` importing `SimClock`, `TICK_DT`, and `MAX_TICKS_PER_FRAME` from `atc_sim.sim.clock`. Encode the five behaviors above as pytest functions using a simple counter closure for `on_tick` (append tick_dt to a list). Use the constants (do not hardcode 0.5 / 5 where a named constant exists). These tests MUST fail at this point because `sim/clock.py` does not exist yet (import error is the expected RED state). Do not implement clock.py in this task.
  </action>
  <verify>
    <automated>python -m pytest tests/test_clock.py -q; test $? -ne 0 && echo "RED confirmed (tests fail as expected — clock not implemented)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_clock.py` defines the five named test functions from the behavior block
    - Running the file fails (RED) — the failure is an ImportError / missing `sim/clock.py`, confirming the tests exercise not-yet-written code
    - Tests reference `TICK_DT` and `MAX_TICKS_PER_FRAME` by name, not magic numbers
  </acceptance_criteria>
  <done>Five failing tests exist that fully specify the accumulator's frame-rate-independence and capped-catch-up behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement SimClock to pass the tests</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-RESEARCH.md (lines 196-233 exact SimClock implementation to copy)
    - .planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-PATTERNS.md (lines 39-71)
    - tests/test_clock.py (created in Task 1)
  </read_first>
  <files>src/atc_sim/sim/clock.py</files>
  <action>
    Implement `src/atc_sim/sim/clock.py` by copying RESEARCH.md lines 196-233 exactly, including the module-level constants `SIM_HZ = 2.0`, `TICK_DT = 1.0 / SIM_HZ`, `MAX_FRAME_TIME = 0.25`, `MAX_TICKS_PER_FRAME = 5`, and the `Callable` import from `collections.abc`. The class exposes `tick_dt`, `_accumulator`, `sim_time`, `dropped_tick_seconds`, and `advance(real_dt, on_tick) -> float`. The advance method: clamps `frame_time = min(real_dt, MAX_FRAME_TIME)`, adds to the accumulator, drains ticks in a `while self._accumulator >= self.tick_dt and ticks_run < MAX_TICKS_PER_FRAME` loop (incrementing sim_time, calling on_tick(tick_dt), subtracting tick_dt), then if the cap was hit AND backlog remains, adds the leftover to `dropped_tick_seconds` and resets `_accumulator` to 0.0; returns `self._accumulator / self.tick_dt`.
    This module MUST NOT import pygame (enforced by tests/test_boundary.py). Both halves of the cap are load-bearing (RESEARCH.md Pitfall 1 / lines 189-193): the frame-time clamp bounds a single slow frame's backlog and the ticks-per-frame cap bounds catch-up work — do not omit either.
  </action>
  <verify>
    <automated>python -m pytest tests/test_clock.py tests/test_boundary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_clock.py -q` passes all five tests (GREEN)
    - `python -m pytest tests/test_boundary.py -q` still passes (clock.py contains no pygame import)
    - `SIM_HZ`, `TICK_DT`, `MAX_FRAME_TIME`, `MAX_TICKS_PER_FRAME` are module-level constants with the researched values (2.0, 0.5, 0.25, 5)
    - `advance()` applies both the frame-time clamp and the ticks-per-frame cap
  </acceptance_criteria>
  <done>SimClock is implemented, all clock tests pass, and the headless boundary guard still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds pure in-memory sim-timing logic with no I/O, no external input, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | (inherited) dependency supply chain | high | mitigate | Covered by plan 01-01's blocking-human legitimacy checkpoint; no new packages installed here. |

No STRIDE-relevant surface is introduced by this plan (offline, single-process, no untrusted input — RESEARCH.md Security Domain).
</threat_model>

<verification>
- `python -m pytest tests/test_clock.py tests/test_boundary.py -q` is fully green
- A single `advance(3.0, on_tick)` call invokes `on_tick` at most 5 times and returns promptly (no hang)
</verification>

<success_criteria>
The fixed-timestep accumulator is implemented and proven frame-rate-independent and stall-safe by passing headless unit tests, satisfying CORE-01 and CORE-02.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-sim-clock-radar-render-loop/01-02-SUMMARY.md` when done
</output>
