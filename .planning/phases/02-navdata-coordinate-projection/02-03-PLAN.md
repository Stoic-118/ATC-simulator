---
phase: 02-navdata-coordinate-projection
plan: 03
type: tdd
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/atc_sim/navdata/models.py
  - src/atc_sim/navdata/eggw.py
  - src/atc_sim/render/window.py
  - src/atc_sim/render/radar.py
  - src/atc_sim/app.py
  - tests/test_navdata.py
  - tests/test_render_smoke.py
autonomous: true
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "The OLNEY 2B SID and DET 2A STAR named fixes render on the radar at their correct real-world lat/lon positions, each with a marker and its 5-letter name"
    - "Each procedure's fixes are connected by a thin line in leg sequence (a visible procedure track)"
    - "Altitude/speed restrictions exist as tested DATA on the fix/leg models (OLNEY at-or-above 6000ft; DET at FL170; ABBOT at FL080, max 220kt), with HEN's leg restriction explicitly None (real chart does not restrict it)"
    - "Each leg's charted (already-magnetic) course_deg_mag is stored as-is and never re-converted through the magnetic boundary"
  artifacts:
    - "OLNEY_2B_SID and DET_2A_STAR Procedure constants in navdata/eggw.py"
    - "Fix, AltitudeRestriction, SpeedRestriction, ProcedureLeg, Procedure models in navdata/models.py"
    - "tests/test_navdata.py procedure assertions (exact coords + restrictions + HEN None)"
  key_links:
    - "render/radar.py projects each ProcedureLeg.fix lat/lon via the same navdata/geo.py projection used for the runway, so fixes and runway share one coordinate frame"
    - "app.py passes [OLNEY_2B_SID, DET_2A_STAR] into build_static_background alongside the runway"
---

<objective>
Second vertical slice: model the real OLNEY 2B SID and DET 2A STAR as frozen Pydantic procedures (named fixes, real coordinates, real altitude/speed restrictions as data) and render their fixes (marker + 5-letter name) connected by a thin procedure track line on the radar — through the same shared projection the runway uses.

Purpose: Deliver NAV-02 (one SID + one STAR with named fixes and restrictions) and the remaining NAV-03 field (per-leg charted `course_deg_mag`), with the second visible payoff (the departure/arrival paths appear where they really are).
Output: extended `navdata/models.py` (procedure models), `navdata/eggw.py` (OLNEY_2B_SID, DET_2A_STAR), extended `render/radar.py`/`window.py`/`app.py`, and procedure tests.
</objective>

<execution_context>
@/home/toby/.claude/gsd-core/workflows/execute-plan.md
@/home/toby/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md
@.planning/phases/02-navdata-coordinate-projection/02-PATTERNS.md
@.planning/phases/02-navdata-coordinate-projection/02-CONTEXT.md
@src/atc_sim/navdata/models.py
@src/atc_sim/navdata/eggw.py
@src/atc_sim/render/radar.py
@src/atc_sim/render/window.py
@src/atc_sim/app.py
@tests/test_navdata.py
@tests/test_render_smoke.py
</context>

## Phase Goal

**As a** controller, **I want to** see EGGW's real runway 25, one SID, and one STAR laid out on the radar at their true geographic positions, **so that** I can work traffic against the same real-world geometry the sim will later use for separation.

> This slice satisfies the SID/STAR half of that story: after Plan 03 the OLNEY 2B departure and DET 2A arrival fixes appear, named and connected, at their correct real-world positions.

## IMPORTANT — runway identifier is "25", restrictions are DATA-ONLY this phase

- Use `"25"` everywhere (see Plan 02's note). Set `Procedure.runway == "25"` for both procedures. 02-RESEARCH.md's model draft shows `"26"` — that is superseded.
- Per CONTEXT.md **D-05**: altitude/speed restrictions are modeled as Pydantic DATA only. Do **NOT** render restriction text on the radar canvas this phase (on-canvas restriction text is deferred to Phase 4 / RADAR-02). Success criterion #2's "each with its modeled altitude/speed restrictions" is satisfied by correct, tested restriction DATA — not by any on-screen text.
- Per Pitfall B: HEN has NO charted altitude restriction — its leg restriction MUST be `None`. Do not fabricate one. OLNEY's stepped DME climb is collapsed to a single `at_or_above 6000ft` on the BNN→OLNEY leg (02-RESEARCH.md "Simplification note", flagged and defensible).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Procedure models + real OLNEY 2B SID / DET 2A STAR data (navdata/models.py, navdata/eggw.py)</name>
  <files>src/atc_sim/navdata/models.py, src/atc_sim/navdata/eggw.py, tests/test_navdata.py</files>
  <read_first>
    - .planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md (section "Heading/Course/Track/Bearing Field Design" — Fix/AltitudeRestriction/SpeedRestriction/ProcedureLeg/Procedure models given verbatim, and the `course_deg_mag` "stored as-charted, NEVER passed through true_to_magnetic" rule; section "Selected Procedures" — the real fix names, DMS→decimal coords, and restriction values for OLNEY 2B and DET 2A; Pitfalls B)
    - .planning/phases/02-navdata-coordinate-projection/02-PATTERNS.md (sections "navdata/models.py" frozen-model + Field pattern, "navdata/eggw.py" DMS-comment-beside-decimal convention)
    - src/atc_sim/navdata/models.py (existing ILS/Runway models from Plan 02 — extend the same file, same frozen/ConfigDict/Field conventions)
    - src/atc_sim/navdata/eggw.py (existing EGGW_RUNWAY + module docstring from Plan 02 — extend the same file)
    - tests/test_navdata.py (existing runway assertions from Plan 02 — add procedure assertions in the same style)
  </read_first>
  <behavior>
    - test_olney_2b_sid_data: OLNEY_2B_SID.name == "OLNEY 2B"; kind == "SID"; runway == "25"; legs have fixes named BNN, HEN, OLNEY in order; BNN fix lat/lon == approx (51.726111, -0.549722); OLNEY fix lat/lon == approx (52.127778, -0.734167); the BNN→OLNEY (final) leg has altitude_restriction kind "at_or_above" altitude_ft 6000; the HEN leg's altitude_restriction is None.
    - test_det_2a_star_data: DET_2A_STAR.name == "DET 2A"; kind == "STAR"; fixes named DET, LOFFO, ABBOT in order; DET lat/lon == approx (51.304003, 0.597275); ABBOT lat/lon == approx (52.016111, 0.599581); DET leg restriction reflects FL170 (altitude_ft 17000, kind "at"); ABBOT leg reflects FL080 (altitude_ft 8000) and a speed_restriction max 220kt.
    - test_procedure_models_reject_bad_fields: `Fix(lat=91.0, ...)` and `AltitudeRestriction(altitude_ft=-100, ...)` raise ValidationError; a Procedure is frozen (assignment raises).
    - test_charted_course_not_double_converted (NAV-03 guard): a ProcedureLeg's `course_deg_mag` equals the exact charted magnetic value stored (assert it is NOT the result of `magnetic_to_true`/`true_to_magnetic` applied to that value — i.e. stored as-is).
  </behavior>
  <action>Extend navdata/models.py with the remaining frozen models from 02-RESEARCH.md exactly: `RestrictionKind = Literal["at", "at_or_above", "at_or_below"]`; `Fix` (`name: str`, `lat: float = Field(ge=-90.0, le=90.0)`, `lon: float = Field(ge=-180.0, le=180.0)`); `AltitudeRestriction` (`kind: RestrictionKind`, `altitude_ft: int = Field(gt=0)`); `SpeedRestriction` (`kind: Literal["max", "min"]`, `speed_kt: int = Field(gt=0)`); `ProcedureLeg` (`fix: Fix`, `course_deg_mag: float | None = Field(default=None, ge=0.0, lt=360.0)`, `altitude_restriction: AltitudeRestriction | None = None`, `speed_restriction: SpeedRestriction | None = None`); `Procedure` (`name: str`, `kind: Literal["SID", "STAR"]`, `runway: str`, `legs: list[ProcedureLeg]`). All `ConfigDict(frozen=True)`. Extend navdata/eggw.py with `OLNEY_2B_SID: Procedure` and `DET_2A_STAR: Procedure` using the exact sourced fix names/coordinates/restrictions from 02-RESEARCH.md "Selected Procedures", preserving each charted DMS string and `[VERIFIED: ...]` source as an inline comment beside the decimal (per 02-PATTERNS eggw convention). Set `runway="25"` on both. OLNEY 2B legs: BNN (no restriction), HEN (altitude_restriction=None — Pitfall B, do NOT invent one), OLNEY (altitude_restriction at_or_above 6000ft — the collapsed terminal step per the Simplification note). DET 2A legs: DET (at FL170 → 17000ft), LOFFO (speed max 250kt), ABBOT (at FL080 → 8000ft, speed max 220kt). Store any charted leg courses as `course_deg_mag` as-charted (magnetic) — never run them through the geo.py magnetic functions. Add the behavior tests to tests/test_navdata.py.</action>
  <verify>
    <automated>pytest tests/test_navdata.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'runway="25"\|runway = "25"' src/atc_sim/navdata/eggw.py` matches both OLNEY_2B_SID and DET_2A_STAR (no "26" identifier values).
    - test_olney_2b_sid_data passes, including the assertion that the HEN leg's altitude_restriction is None (Pitfall B — real absent restriction, not fabricated).
    - test_det_2a_star_data passes with the exact DET/LOFFO/ABBOT coordinates and FL170/FL080 + 220kt restrictions.
    - test_procedure_models_reject_bad_fields and test_charted_course_not_double_converted pass.
  </acceptance_criteria>
  <done>OLNEY 2B and DET 2A are modeled as frozen procedures with real fix coordinates and real restriction data (HEN unrestricted), all under runway "25", proven by exact-value tests.</done>
</task>

<task type="auto">
  <name>Task 2: Render SID/STAR fixes (marker + name) and thin procedure track lines on the radar (_draw_procedure)</name>
  <files>src/atc_sim/render/window.py, src/atc_sim/render/radar.py, src/atc_sim/app.py, tests/test_render_smoke.py</files>
  <read_first>
    - src/atc_sim/render/radar.py (Plan 02's build_static_background(size, runway), world_to_screen, _draw_runway, and the navdata import group to extend; draw_aircraft z-order convention lines 72-94)
    - src/atc_sim/render/window.py (palette block incl. Plan 02's RUNWAY_COLOR — add the new fix/procedure colors in the same style)
    - src/atc_sim/app.py (Plan 02's `build_static_background(screen.get_size(), EGGW_RUNWAY)` call to extend)
    - .planning/phases/02-navdata-coordinate-projection/02-PATTERNS.md (sections "render/radar.py" Z-order/text-rendering guidance + "No Analog Found" — text rendering is a NEW pattern: load a pygame.font once at module scope, cache it, `font.render()` then blit; waypoint marker via pygame.draw.circle or pygame.draw.polygon)
    - .planning/phases/02-navdata-coordinate-projection/02-CONTEXT.md (D-01 fix marker+name style, D-04 thin low-clutter track line, D-05 NO restriction text this phase)
    - .planning/phases/02-navdata-coordinate-projection/02-RESEARCH.md ("Architectural Responsibility Map" — fix/track rendering is render-tier, reads navdata+projection output only)
    - tests/test_render_smoke.py (Plan 02's extended smoke test to further extend with procedures)
  </read_first>
  <action>In render/window.py, add `FIX_COLOR`, `FIX_TEXT_COLOR`, and `PROCEDURE_LINE_COLOR` to the D-01 palette block as `(R,G,B)` tuples with inline comments, staying within the dark-flat EFIS palette (CONTEXT D-01/D-04 — thin, low-clutter, not competing with the cyan aircraft symbol). In render/radar.py, import the three new colors and add `from atc_sim.navdata.models import Procedure` to the existing navdata import group. Load a fix-label font once at module scope (e.g. `_FIX_FONT = None` lazily initialized after pygame.font.init(), or a module-level init helper) — text rendering is new to this codebase (02-PATTERNS "No Analog Found"): follow the load-once/cache/`font.render()`+blit idiom, consistent with build_static_background's compute-once philosophy. Add `_draw_procedure(surface, procedure)` that projects each `leg.fix` lat/lon via `project_to_local_xy_nm` → `world_to_screen`, then draws in this z-order (mirroring draw_aircraft's discipline): (1) the thin procedure track line connecting consecutive fixes in leg order (PROCEDURE_LINE_COLOR, width 1 / aaline — thin per D-04), (2) each fix marker (small dot via pygame.draw.circle in FIX_COLOR), (3) each fix's 5-letter name text (FIX_TEXT_COLOR) blitted beside its marker — so text is never occluded by the line/marker. Do NOT render any altitude/speed restriction text (D-05 — deferred to Phase 4). Change `build_static_background`'s signature to `build_static_background(size, runway, procedures)` and, after `_draw_runway`, loop `for proc in procedures: _draw_procedure(surface, proc)` (Pattern 1 — same cached surface). In app.py, `from atc_sim.navdata.eggw import EGGW_RUNWAY, OLNEY_2B_SID, DET_2A_STAR` and call `build_static_background(screen.get_size(), EGGW_RUNWAY, [OLNEY_2B_SID, DET_2A_STAR])`. Extend tests/test_render_smoke.py to pass a procedures list (the real constants or an inline fixture) through the new signature so `_draw_procedure` (including font render) runs headlessly under SDL_VIDEODRIVER=dummy.</action>
  <verify>
    <automated>pytest tests/test_render_smoke.py tests/test_navdata.py tests/test_projection.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FIX_COLOR\|FIX_TEXT_COLOR\|PROCEDURE_LINE_COLOR' src/atc_sim/render/window.py` shows all three new constants in the palette block.
    - `grep -n 'def _draw_procedure' src/atc_sim/render/radar.py` matches.
    - `grep -n 'build_static_background(screen.get_size(), EGGW_RUNWAY, \[OLNEY_2B_SID, DET_2A_STAR\])' src/atc_sim/app.py` matches (both procedures wired in).
    - No restriction-text rendering: `_draw_procedure` renders only fix names (5-letter identifiers), not altitude/speed values (D-05).
    - test_render_smoke passes headlessly with fixes, names, and track lines drawn; full `pytest` suite green.
  </acceptance_criteria>
  <done>Launching the app shows OLNEY 2B and DET 2A fixes as markers with 5-letter names, connected by thin procedure track lines, at their correct real-world positions — sharing the runway's projection frame.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hand-authored procedure data → sim core | Real fix coordinates/restrictions transcribed by hand; a typo or a fabricated restriction silently corrupts "real" navdata |
| sim core (navdata) → render | render reads frozen procedures + projection output only; never mutates them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering (data integrity) | navdata/eggw.py hand-transcribed fix coords/restrictions | medium | mitigate | Pydantic Field bounds on lat/lon/altitude/speed; test_olney_2b_sid_data / test_det_2a_star_data assert each exact sourced value; test asserts HEN restriction is None so a fabricated restriction fails CI (Pitfall B) |
| T-02-04 | Tampering | render mutating frozen procedures | low | mitigate | `ConfigDict(frozen=True)` on all procedure models; render reads only |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan (geographiclib handled in Plan 02-01) |
</threat_model>

<verification>
- `pytest tests/test_navdata.py tests/test_projection.py tests/test_render_smoke.py -x` all green.
- `pytest` full suite green.
- Manual: launching `atc-sim` shows both procedures' fixes (named) connected by thin tracks at real positions (formal visual sign-off is Plan 05).
</verification>

<success_criteria>
NAV-02 satisfied: one SID (OLNEY 2B) and one STAR (DET 2A) modeled with named fixes/waypoints and altitude/speed restrictions as tested data, rendered as markers + names + thin procedure tracks on the radar at correct real-world positions; NAV-03's per-leg charted course field present and never double-converted.
</success_criteria>

## Artifacts this phase produces (Plan 03)

Modified files (new symbols):
- `src/atc_sim/navdata/models.py` — new symbols: `RestrictionKind`, `Fix`, `AltitudeRestriction`, `SpeedRestriction`, `ProcedureLeg`, `Procedure`
- `src/atc_sim/navdata/eggw.py` — new symbols: `OLNEY_2B_SID`, `DET_2A_STAR`
- `src/atc_sim/render/window.py` — new constants: `FIX_COLOR`, `FIX_TEXT_COLOR`, `PROCEDURE_LINE_COLOR`
- `src/atc_sim/render/radar.py` — new function `_draw_procedure()`; module-level fix-label font cache (e.g. `_FIX_FONT`); changed signature `build_static_background(size, runway, procedures)`
- `src/atc_sim/app.py` — imports `OLNEY_2B_SID`, `DET_2A_STAR`; passes procedures list to `build_static_background`
- `tests/test_navdata.py` — new symbols: `test_olney_2b_sid_data`, `test_det_2a_star_data`, `test_procedure_models_reject_bad_fields`, `test_charted_course_not_double_converted`
- `tests/test_render_smoke.py` — updated to pass procedures through `build_static_background`

<output>
Create `.planning/phases/02-navdata-coordinate-projection/02-03-SUMMARY.md` when done.
</output>
